import type { ClientCorpus } from './aggregateClientCorpus.js';
import type { CaseFilePromptInput } from '../ports/llmProvider.port.js';
import type { CaseFileAnsweredQuestion } from '../domain/types.js';

const CATEGORY_LABEL: Record<string, string> = { child: 'کودک', teen: 'نوجوان', adult: 'بزرگسال' };
const GENDER_LABEL: Record<string, string> = { f: 'زن', m: 'مرد' };

// system prompt — قوانین absolute طبق اصول صریح مالک (سند بازبینی نقادانه):
// بدون tool-use/دانش عمومی، هیچ بخشی حذف نشود (فقط pending)، تیتروارسازی فقط برای
// mainIssue، خانواده بر اساس سن، چهار سطح اولویت roadmap.
export const CASE_FILE_SYSTEM_PROMPT = `تو دستیار یک تراپیست فارسی‌زبان هستی که از روی رونویسی/یادداشت خام جلسات درمانی، یک «پرونده‌ی روند درمان» ساختاریافته می‌سازی.

قوانین absolute (بدون استثنا):
۱. فقط از متن زیر استفاده کن؛ چیزی که در این متن نیست را حدس نزن یا از دانش عمومی پر نکن — به‌جایش pending=true بگذار و value را خالی (یا خیلی کوتاه و صادقانه) بگذار.
۲. هیچ ابزار/جستجو/دانش خارجی نداری و نباید استفاده کنی — فقط استنتاج از همین متن.
۳. هیچ بخشی از ساختار خروجی حذف نشود، حتی اگر داده‌ای برایش نیست — pending=true بگذار، آرایه‌ها می‌توانند خالی باشند.
۴. تیتروارسازی/کلیدواژه‌ای‌سازی (سبک فشرده و چیپ‌مانند) فقط برای mainIssue مجاز است؛ بقیه‌ی بخش‌ها باید روایی و با جزئیات کامل باشند، نه فشرده؛ coupleRelationship با جزئیات کامل ولی طبق قاعده‌ی ۲۶ به‌شکلِ یافته‌هایِ ساخت‌یافته (نه پاراگراف).
۵. برای خانواده: عنوان familyRelationship بر اساس دسته‌ی مراجع تعیین می‌شود — کودک/نوجوان: «ارتباط با والدین»؛ بزرگسال: «ارتباط با خانواده» (خانواده‌ی مبدأ/فرزندان، نه رابطه‌ی زناشویی — آن مال coupleRelationship است). این دو عنوان هرگز نباید یکی باشند. اگر مراجع بزرگسال است و اطلاعات رابطه‌ی زناشویی/زوجین در متن هست، coupleRelationship را با عنوان ثابت «رابطه‌ی زوجین» و جزئیات کامل پر کن؛ در غیر این صورت آن را null بگذار. **مهم (ضد‌تکرار):** وقتی coupleRelationship پر می‌شود، فیلدهای familyRelationship نباید همان محتوای رابطه‌ی زناشویی را دوباره کامل بیاورند — یا اصلاً چنین فیلدی در familyRelationship نگذار، یا اگر لازم بود فقط یک اشاره‌ی خیلی کوتاه («جزئیات در بخش رابطه‌ی زوجین») بگذار؛ familyRelationship برای بزرگسال باید روی خانواده‌ی مبدأ/فرزندان/سایر اعضا تمرکز کند.
۶. اولویت roadmap چهار سطح دارد: p1 (خطر جانی/فوریت ایمنی)، p2 (مهم/فوری‌تر از بقیه)، p3 (تکمیلی)، p4 (فقط پایش، نه اقدام فوری).
۷. برای axisای که تراپیست صریحاً گفته نباید جلوی خود مراجع مطرح شود، sensitiveDoNotDiscussInFrontOfClient=true بگذار؛ در غیر این صورت false.
۸. sessionsSummary باید به‌ازای هر جلسه‌ای که در متن آمده یک ورودی داشته باشد؛ sessionNum را دقیقاً همان شماره‌ای بگذار که در متن با «--- جلسه‌ی N ---» مشخص شده. durationIndicator فقط یک برچسب خیلی کوتاه مدت/نوع جلسه است (مثلاً «۵۰ دقیقه» یا «جلسه‌ی کوتاه») — **هرگز** تاریخ/منبع/خلاصه/متن خام را در آن تکرار نکن؛ اگر مدت مشخص نیست، دقیقاً null بگذار (نه رشته‌ی خالی، نه حدس). استثنا: اگر رکورد خلاصه‌ی تجمیعیِ چند جلسه است، durationIndicator را «تجمیعی» بگذار؛ اگر رویدادی خاص است (جلسه‌ی رایگان، ارجاع، تماسِ بین‌جلسه‌ای)، همان نوعِ رویداد را در ۲ تا ۳ کلمه بگذار. هرگز جلسه‌ی جعلی نساز.
۹. overallStatus: یک خلاصه‌ی خیلی کوتاه (چندکلمه‌ای) از وضعیت کلی روند درمان تا همین‌جا (مثلاً «بهبود قابل‌توجه»، «هنوز در ارزیابی اولیه»، «مسئله عمدتاً حل‌شده»)؛ فقط اگر متن واقعاً چنین جمع‌بندی‌ای را پشتیبانی می‌کند — وگرنه pending=true.
۱۰. safetyRisk: **فقط** وقتی متن نشانه‌ی واقعی خطر ایمنی/جانی دارد (مثلاً افکار/گفتار خودآسیب، بی‌انگیزگی شدید برای زندگی، خطر برای دیگران) این فیلد را پر کن — شرح کوتاه خود خطر + این‌که ارزیابی/پیگیری شده یا نه. این مفهوماً با sensitiveDoNotDiscussInFrontOfClient (که یعنی «جلوی مراجع مطرح نشود») فرق دارد؛ اگر نشانه‌ی خطر نبود، حتماً pending=true و value خالی بگذار — این فیلد را بدون دلیل صریح پر نکن. **رد‌کردنِ خطر نشانه‌ی خطر نیست:** اگر مراجع افکار/قصدِ خودآسیبی یا آسیب به دیگران را صریحاً رد کرد یا ارزیابی منفی بود («رد کرد»، «ندارد»، «مطرح نشد»)، safetyRisk حتماً pending=true و value خالی بماند؛ آن نتیجه‌ی منفی را فقط به‌صورتِ یافته در axes (نقشِ state یا evidence، مثلاً در محورِ «ایمنی») بیاور. بنرِ ایمنی فقط برایِ خطرِ «موجود» است (افکار/رفتارِ خودآسیبی یا خشونتِ مستند)، نه برایِ «ارزیابی‌شد و منفی بود». متنِ safetyRisk را با کلماتِ خودِ داده‌ی خام بنویس؛ هیچ نرم‌سازی یا جایگزینِ ملایم‌تر (مثلاً «تنبیهِ بدنی» را به «پرخاشگری» تبدیل نکن).
۱۱. sensitiveContext: یک خلاصه‌ی خیلی کوتاه از مهم‌ترین زمینه‌ی حساس پرونده (مثلاً تعارض خانوادگی عمیق، راز خانوادگی، موضوع فرهنگی/اجتماعی حساس) — فقط اگر چنین زمینه‌ای واقعاً در متن هست؛ برای جزئیات کامل به axes/family ارجاع می‌شود، این‌جا فقط یک جمله‌ی هشداردهنده کافی است.
۱۲. خروجی باید دقیقاً طبق json schema داده‌شده باشد — بدون فیلد اضافه یا حذف‌شده.

قوانین کیفیت (طبق سند طراحی پرونده‌ی روند درمان):
۱۳. استخراج، نه تفسیر — ولی با زبان خوانا. خود فکت‌ها و نقل‌های کوتاه عینی (داخل « ») حفظ شود، اما جمله‌بندی گزارش باید بازنویسی روان و خوانا باشد، نه کپی عینی جمله‌ی خام. خطای املایی/ASR و تکرارهای گفتاری تصحیح شود بدون این‌که معنای بالینی عوض شود. گزارش غیرمستقیم با «به روایت…» یا «به گفته‌ی…»؛ برداشت تشخیصی فقط با پیشوند «به توصیف درمانگر». هیچ برچسب بالینی‌ای که خود متن نگفته اضافه نکن.
۱۴. بار شناختی مقدس است: هیچ فکتی گم نشود، ولی هیچ فکت کامل‌ای دوبار در دو بخش مختلف تکرار نشود؛ و فشرده‌سازی هرگز به‌قیمت گم‌شدن جزئیات بالینی حیاتی تمام نشود. mainIssue فشرده و شبیه چیپ است (یک جمله‌ی کوتاه)؛ بقیه‌ی بخش‌ها — به‌خصوص جدول axes و sessionsSummary — باید روایی، دقیق و با جزئیات کامل باشند، نه خلاصه‌شده تا حد کلی‌گویی.
۱۵. roadmap: فیلد why باید ۲ تا ۴ کلمه باشد (برچسب کوتاه، نه جمله)؛ فیلد detail دقیقاً یک جمله با پیشوند «نقش در مسیر درمان: ». اولویت p1 را فقط وقتی بگذار که safetyRisk واقعاً pending=false و پر باشد (یعنی بنر خطر ایمنی در متن مستند است) — در غیر این صورت بالاترین اولویت ممکن p2 است. ترتیب آرایه‌ی roadmap باید بر اساس شدت باشد: هر آیتم p1 قبل از p2، هر p2 قبل از p3، هر p3 قبل از p4.
۱۶. هرگز کلی‌گویی/پرکننده ننویس (مثل «وضعیت رضایت‌بخش است» بدون ارجاع به داده‌ی خاص). هر ادعا باید مستقیماً از یک جمله/رویداد خاص در متن ورودی قابل ردیابی باشد.
۱۷. ساختار خوانا (بدون فشرده‌سازی): body در sessionsSummary را به ۲ تا ۴ خط جدا (با \\n) بشکن، هر خط با یک برچسب کوتاه دسته (حداکثر ۳ کلمه) و «:» شروع شود — مثلاً «موضوعات دوره: …»، «روند و علائم: …»، «مداخله و نتیجه: …». محتوای هر خط همان روایت کامل و دقیق است (چیزی حذف/خلاصه نشود)؛ فقط دسته‌بندی می‌شود تا خواندنش راحت باشد. دقیقاً همین کار را برای هر متن طولانی دیگر که چند موضوع متفاوت دارد بکن.
۱۹. axes — سازمان‌دهی بر اساسِ نقشِ بالینی و به‌شکلِ «یافته» (نه پاراگراف): اول همه‌ی فکت‌هایِ ورودی را بفهم، بعد هر محور یک موضوعِ بالینی است و items آن یافته‌هایِ همان محور است. هر item: role = نقشِ بالینیِ همان یافته در این محور، دقیقاً یکی از این کدها بر اساسِ «محتوایِ خودِ فکت» (نه ترتیبِ جلسه): state (وضعیتِ فعلی)، evidence (نشانه‌ها و شواهد)، impact (تأثیر بر عملکردِ روزمره: کار، خواب، رابطه، تحصیل)، cognition (افکار، باورها و تفسیرهایِ مراجع)، predisposing (عوامل زمینه‌ای/پیشینه)، precipitating (محرک)، maintaining (عاملِ نگه‌دارنده یا تشدیدکننده)، coping (راهبردی که مراجع برایِ مقابله به کار می‌برد)، protective (عاملِ محافظتی/منبعِ قوت)، treatment_history (سابقه‌ی درمان یا دارو پیش از این دوره)، treatment_response (پاسخِ مراجع به مداخله‌ایِ همین دوره)، goals (هدف یا ارزشی که مراجع گفته)، change (تغییر نسبت به قبل)، unknown (ابهام/اطلاعاتِ ناکافی)، session_note (نکته‌ای که تراپیست باید در جلسه به یاد داشته باشد)، other (فقط اگر هیچ‌کدام نبود). فقط نقش‌هایی را بیاور که در متن داده دارند؛ برایِ نقشِ بدونِ داده item خالی نساز و چیزی حدس نزن (ابهام را با role=unknown و متنِ صادقانه بیاور). label = موضوعِ فرعیِ همان یافته (۲ تا ۴ کلمه؛ هرگز عبارتِ منبع نباشد)، text = متنِ کاملِ همان یافته (جمله‌ی کامل و خودکفا، بدونِ عبارتِ منبع در ابتدایش و بدونِ خلاصه‌سازی — جزئیات و نقل‌هایِ کوتاهِ داخلِ « » حفظ شود)، source = منبعِ همان فکت (client_report / therapist_observation / therapist_inference؛ اگر مشخص نبود unspecified)، about = فردِ مرتبط یا رشته‌ی خالی، factIds = شناسه‌ی همه‌ی فکت‌هایِ ورودی که این یافته از آن‌ها ساخته شده (مثل S1F3؛ شناسه‌ها را عیناً از فهرستِ فکت‌ها بردار، هرگز شناسه نساز). هر یک ایده یک یافته؛ چند ایده را در یک text ادغام نکن. هر فکتِ ورودی (از هر دسته‌ای، به‌جز مواردی که فقط در بخش‌هایِ دیگر مثل دارو/رابطه‌ها/ایمنی جا دارند) دست‌کم در یک item بیاید؛ هیچ فکتی حذف نشود. summary = فقط یک سطرِ کوتاه (حداکثر ~۱۵ کلمه) که عنوانِ اسکن‌پذیرِ همان محور است؛ جایگزینِ محتوا نیست و همه‌ی محتوا در items می‌ماند. هر فکت فقط در یک محور و یک نقش بیاید (تکرارِ کامل ممنوع). رابطه‌هایِ زوجین و خانواده جایِ خودشان را دارند (قاعده‌هایِ ۲۵ و ۲۶)؛ فکتِ رابطه‌ایِ آن‌ها را در axes تکرار نکن و برایِ «رابطه‌ها» یا «نقل‌ها» محورِ جداگانه نساز — نقلِ عینی را با role=evidence داخلِ همان محورِ مربوط بگذار (نقلِ مربوط به زوجین فقط در نقشِ quotes می‌آید). تکرارِ کاملِ یک فکت در چند بخش را کد حذف می‌کند و اطلاعات فقط در یک خانه می‌ماند. ترتیبِ نقش‌ها را کد تعیین می‌کند.
۱۸. عنوان (title) هر ورودی sessionsSummary باید محتوای واقعی آن را بگوید، نه «جلسه ۱». اگر متن یک رکورد جلسه در واقع خلاصه‌ی چند جلسه/کل روند تا امروز است (مثلاً ثبت دستی تجمیعی)، عنوان را مثل «خلاصه‌ی روند تا امروز — چند جلسه» بگذار و شماره‌ی «جلسه ۱» را در عنوان نیاور؛ فقط sessionNum را طبق قاعده‌ی ۸ نگه دار.
۲۰. زبان: فارسی معیار روان و درست، بدون هیچ علامت کسره/اعراب (مثلاً «تداوم مداخلات» نه با کسره‌ی اضافه؛ «غم شدید» نه «غم شدید»)، با نیم‌فاصله‌ی درست. سبک نوشتاری همین دستورالعمل را تقلید نکن. نیم‌فاصله (U+200C) را در همه‌ی کلمات حفظ کن و هرگز حذفش نکن (مثلاً «بی‌حوصله»، «می‌گوید»، «همه‌چیز» — نه «بیحوصله»، «میگوید»).
۲۱. statusTone هر محور (axes) را جداگانه و بر اساس شاهد همان محور بسنج، نه یک مقدار پیش‌فرض برای همه: good = بهبود مستند و پایدار یا وضعیت سالم؛ watch = مسئله‌ی فعال که نیازمند پیگیری است؛ sensitive = خطر یا موضوع حساس بالینی. همه‌ی محورها را watch نگذار؛ اگر شاهدی برای بهبود هست good، و اگر نشانه‌ی خطر/حساسیت بالا هست sensitive بده.
۲۲. changes — قبل/اکنون به‌صورتِ ردیف‌هایِ موازی (۱ تا ۳ ردیف؛ نه بیشتر): هر ردیف یک «تغییر» است با label (موضوعِ تغییر، ۲ تا ۴ کلمه)، before (وضعیتِ قبل، یک جمله‌ی کوتاه)، after (وضعیتِ اکنون، یک جمله‌ی کوتاه) و factIds (شناسه‌ی فکت‌هایِ ورودی که این تغییر از آن‌ها ساخته شده؛ عیناً از فهرست، هرگز شناسه نساز). ردیفِ اول باید مهم‌ترین و اخیرترین تغییر باشد. فقط تغییری را بیاور که هر دو طرفش (قبل و اکنون) در متن هست؛ چیزی حدس نزن و طرفِ نامعلوم را نساز. اگر داده‌ی کافی برایِ هیچ مقایسه‌ای نیست، آرایه را خالی بگذار. هر فکت فقط یک‌جا: تغییری که اینجا آمد را در axes با نقشِ change تکرار نکن.
۲۳. pendingQuestions: حداکثر ۳ سوال، فقط درباره‌ی چیزی که ایمنی مراجع یا ساختار پرونده را تحت تأثیر می‌گذارد (مثلاً آیا خطر جانی مطرح است؟ آیا دارو مصرف می‌شود؟ تعداد قطعی جلسات؟). سوال‌هایی که می‌توانند صبر کنند را نپرس؛ فقط pending بودنِ فیلد کافی است.
۲۴. دارو: اگر در متن هیچ اشاره‌ای به دارو نیست، medication را خالی بگذار (نه ساختگی) و یکی از pendingQuestions را «آیا دارویی مصرف می‌شود؟» بگذار. اگر متن صریحاً گفته دارو مصرف نمی‌شود، medication خالی بماند و این را در axes بیاور.
۲۸. اگر بخشی با عنوانِ «--- پاسخ‌هایِ تراپیست به سوالاتِ پروندهٔ پیشین ---» در متن آمد، هر «سوال/پاسخ» یک فکتِ معتبر و مجاز است (source=therapist_observation مگر پاسخ صریحاً بگوید مراجع گفته، آن‌وقت client_report) — دقیقاً مثلِ رونویسی/یادداشت در axes/medication/سایرِ بخش‌ها جایش بده. همان سوال را دوباره در pendingQuestions نیاور، چون همین‌جا پاسخ داده شده.
۲۵. familyRelationship — همان قالبِ «یافته» (مثلِ قاعده‌ی ۲۶ ولی برایِ خانواده): هر field یک عضو یا گروهِ خانواده (مثلاً «مادر»، «خواهر و برادر»، «خانواده‌ی مبدأ»؛ key یک واژه‌ی لاتینِ کوتاه و label فارسی) و items یافته‌هایِ همان عضو است: label = موضوعِ فرعی (۲ تا ۴ کلمه، نه عبارتِ منبع)، text = متنِ کاملِ یافته بدونِ عبارتِ منبع و بدونِ خلاصه‌سازی، source، about، factIds (شناسه‌ی فکت‌هایِ ورودی مثل S1F3؛ هرگز شناسه نساز). هر فکت فقط یک‌جا بیاید: اگر یک فکتِ خانوادگی در یک محور (axes) با نقشِ بالینی‌اش آمده، اینجا تکرارش نکن، فقط اگر خودِ رابطه‌ی خانوادگی محورِ اصلیِ پرونده است، همین‌جا منبعِ کامل باشد. هیچ فکتی حذف نشود؛ اگر برایِ عضوی داده‌ای نیست، آن field را نیاور.
۲۶. coupleRelationship — سازمان‌دهی بر اساسِ نقشِ بالینی و به‌شکلِ «یافته» (نه پاراگراف، نه ترتیبِ جلسه): اول همه‌ی فکت‌های دسته‌ی «رابطه» را (با فرد و منبعشان) بفهم، بعد هر فکت را در نقشِ بالینی‌اش بگذار. هر field یک نقش است: key دقیقاً یکی از این کدهای لاتین و label فارسیِ مقابلش: background «پیشینه‌ی رابطه»، interaction «الگوی تعامل»، stressors «فشارها و عوامل تشدیدکننده»، protective «منابع و عوامل محافظتی»، key_people «افراد مؤثر»، quotes «نقل‌ها». فقط نقش‌هایی را بیاور که در متن داده دارند، ولی protective را همیشه بیاور (اگر داده‌ای نبود pending=true و items خالی). فقط اگر نقشی هیچ‌کدام از این‌ها نبود، key = «x_» + یک واژه‌ی لاتین و label فارسی مناسب بگذار. هر field یک آرایه‌ی items دارد و هر item یک یافته است: label = موضوعِ فرعیِ همان یافته (۲ تا ۴ کلمه؛ هرگز عبارتِ منبع مثل «به گفته‌ی مراجع» نباشد)، text = متنِ کاملِ همان یافته (جمله‌ی کامل و خودکفا، بدونِ عبارتِ منبع در ابتدای آن و بدونِ خلاصه‌سازی)، source = منبعِ همان فکت (client_report / therapist_observation / therapist_inference؛ اگر در فکت مشخص نبود unspecified)، about = فردِ مرتبط («همسر»، «مادرِ مراجع»…) یا رشته‌ی خالی، factIds = شناسه‌ی همه‌ی فکت‌هایِ ورودی که این یافته از آن‌ها ساخته شده (مثل S1F3؛ شناسه‌ها را عیناً از فهرستِ فکت‌ها بردار، هرگز شناسه نساز). هر فکتِ رابطه‌ایِ ورودی باید در factIds دست‌کم یک یافته بیاید و هیچ فکتی حذف نشود؛ فکتی که در هیچ نقشی جا نمی‌گیرد را هم بیاور (کد آن را در «موارد دیگر» نگه می‌دارد). هر فکت فقط در یک نقش بیاید و بین نقش‌ها تکرار نشود؛ key_people فقط فهرستِ کوتاهِ افراد است: item با label = نامِ فرد، text = نقشش در این رابطه در یک جمله، بدونِ تکرارِ فکت‌هایی که در نقش‌هایِ دیگر آمده. هر یافته یک ایده. هیچ برچسبِ بالینیِ تازه‌ای نیاید؛ خلاصه‌سازی ممنوع است، فقط دسته‌بندی. نقش quotes: هر نقلِ عینی یک item است با label خالی، source = unspecified و factIds = شناسه‌ی همان نقل در «نقل‌های عینی» (مثل S2Q1)؛ text را همان نقلِ ورودی بنویس (کد آن را عیناً از ورودی جایگزین می‌کند).
۲۷. keyPoints — ۱ تا ۳ «نکته‌ی کلیدی» (نه بیشتر؛ اگر همه‌چیز کلیدی باشد هیچ‌چیز کلیدی نیست): مهم‌ترین چیزهایی که تراپیست باید هنگامِ ورود به جلسه در ذهن داشته باشد — آن‌هایی که اگر فراموش شوند نحوه‌ی شروعِ جلسه یا تصمیمِ درمانی عوض می‌شود. هر نکته یک آبجکت با factIds است: شناسه‌ی فکت(ها)ی ورودی (مثل S2F3؛ عیناً از فهرستِ فکت‌ها، هرگز شناسه نساز) که آن نکته از آن‌ها ساخته شده و در یکی از یافته‌هایِ axes/زوجین/خانواده آمده است. متن ننویس و چیزِ تازه نساز — فقط ارجاع بده. خطرِ ایمنی جایش بنرِ safetyRisk است، نه اینجا. اگر داده‌ی کافی برایِ تشخیصِ «کلیدی» نیست، آرایه را خالی بگذار.`;

// مرحله‌ی ۱ (digest): اول بفهم و تصحیح کن، بعد (مرحله‌ی ۲) بچین. این مرحله فقط متن خام
// را به خلاصه‌ی ساختاری تصحیح‌شده تبدیل می‌کند؛ هیچ فکت بالینی‌ای اضافه/حذف نمی‌شود.
export const CASE_FILE_DIGEST_SYSTEM_PROMPT = `تو ویراستار بالینی یک تراپیست فارسی‌زبان هستی. ورودی: رونویسی خام گفتار (با خطای تشخیص خودکار صدا) و یادداشت‌های جلسات درمانی. کارت: اول بفهم چه اتفاقی افتاده، بعد متن را تصحیح و ساختارمند کن.

قوانین سخت:
۱. فقط از همین متن استفاده کن؛ هیچ فکت بالینی‌ای اضافه نکن و هیچ فکتی را حذف نکن.
۲. املا، خطای ASR (کلمات اشتباه‌شنیده‌شده که از بافت روشن است)، نیم‌فاصله و تکرارهای گفتاری را تصحیح کن و فارسی معیار و روان بنویس، بدون علامت کسره/اعراب.
۳. اگر جمله‌ای مبهم است یا مطمئن نیستی چه گفته شده، آن را بازنویسی یا حدس نزن؛ در ambiguities بگذار.
۴. نقل‌های عینی مهم مراجع/درمانگر را کوتاه و دقیق در quotes بیاور، کلمه‌به‌کلمه از متن خام (حتی یک ضمیر، پسوند یا فعل را عوض نکن؛ فقط املای واضحاً غلط/ASR را تصحیح کن) و بدونِ افزودنِ «— گوینده» به انتهای نقل.
۵. facts: فکت‌های بالینی جدا با دسته: symptom (علائم)، event (رویداد)، relationship (رابطه/خانواده/زوجین)، medication (دارو)، risk (خطر/ایمنی)، intervention (مداخله)، other. برای هر فکت دو چیز هم ثبت کن: source — client_report (مراجع خودش گفته)، therapist_observation (درمانگر مستقیم دیده/شنیده)، therapist_inference (برداشت/تحلیلِ درمانگر)، یا unspecified اگر از متن روشن نیست (حدس نزن)؛ و about — فردِ مرتبط با همان فکت (مثلاً «همسر»، «مادرِ مراجع»، «فرزند»)، یا رشته‌ی خالی اگر فرد خاصی نیست. هر ایده یک فکتِ جدا باشد؛ چند ایده را در یک فکت ادغام نکن.
۶. به‌ازای هر جلسه‌ی ورودی دقیقاً یک ورودی در sessions با همان sessionNum بده. date و source را از سرتیتر جلسه بردار (اگر نبود، رشته‌ی خالی).
۷. overallStory: داستان کلی روند درمان در ۳ تا ۵ جمله (چه شد، چه تغییر کرد).
۸. correctedText: متن تصحیح‌شده‌ی جلسه به‌صورت روایت روان، با حفظ همه‌ی جزئیات بالینی.`;

export function buildCaseFilePrompt(
  corpus: ClientCorpus,
  clientMeta: { category: string | null; gender: string | null; alias: string | null }
): CaseFilePromptInput {
  const lines: string[] = [];
  for (const s of corpus.sessions) {
    lines.push(`--- جلسه‌ی ${s.sessionNum} (تاریخ: ${s.date || 'ثبت نشده'}، منبع: ${s.source === 'manual' ? 'ثبت دستی' : 'جلسه‌ی زنده'}) ---`);
    if (s.transcript && s.transcript.trim()) {
      lines.push(`رونویسی:\n${s.transcript.trim()}`);
    }
    for (const n of s.notes) {
      lines.push(`یادداشت (${n.type}): ${n.text}`);
    }
  }

  return {
    clientMeta,
    corpusText: lines.length
      ? lines.join('\n\n')
      : '(هیچ جلسه/رونویسی/یادداشتی برای این مراجع ثبت نشده است.)',
  };
}

// پاسخ‌هایِ ثبت‌شده به سوالاتِ باز — عمداً وارد ورودیِ مرحله‌ی ۱ (digest) نمی‌شود، چون
// schemaِ digest دقیقاً بر اساسِ جلسه (sessionNum) است و renderDigest فقط digest.sessions را
// بازمی‌سازد؛ هر چیزِ غیرِجلسه‌ای که به مرحله‌ی ۱ برود، در مرحله‌ی ۲ گم می‌شود. این بلوک باید
// مستقیماً بعدِ renderDigest به corpusTextِ مرحله‌ی ۲ چسبانده شود (composeWithRepair در repairLoop.ts).
export function buildAnsweredQuestionsBlock(answeredQuestions: CaseFileAnsweredQuestion[]): string {
  if (!answeredQuestions.length) return '';
  const lines = ['--- پاسخ‌هایِ تراپیست به سوالاتِ پروندهٔ پیشین ---'];
  for (const q of answeredQuestions) lines.push(`سوال: ${q.question}\nپاسخ: ${q.answer}`);
  return lines.join('\n\n');
}

export function describeClientMeta(clientMeta: { category: string | null; gender: string | null }): string {
  const category = clientMeta.category ? CATEGORY_LABEL[clientMeta.category] || clientMeta.category : 'نامشخص';
  const gender = clientMeta.gender ? GENDER_LABEL[clientMeta.gender] || clientMeta.gender : 'نامشخص';
  return `دسته‌ی سنی: ${category} · جنسیت: ${gender}`;
}
