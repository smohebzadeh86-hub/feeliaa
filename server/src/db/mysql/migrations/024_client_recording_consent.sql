-- رضایتِ ضبط و رونویسی «یک بار برایِ هر مراجع» (دستورِ مالک، 2026-09-24): «برای هر مراجع فقط یه بار رضایت بده».
-- NULL = هنوز ثبت نشده (رفتارِ قبلی: پیش از جلسه/آپلود پرسیده می‌شود). مقدار = زمانِ اولین رضایتِ صریح
-- (تیکِ تراپیست پیش از جلسه‌ی زنده یا آپلودِ فایل). لغو ⇒ دوباره NULL.
-- عمداً additive و بدونِ backfill: رضایتِ «همان جلسه»ی قدیمی به‌معنایِ رضایتِ دائمی تفسیر نمی‌شود.
ALTER TABLE clients ADD COLUMN recording_consent_at DATETIME NULL;
