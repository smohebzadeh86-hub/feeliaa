-- 014: تاریخِ جلسه اختیاری می‌شود — فقط برایِ جلسه‌ی دستی (source='manual') کاربرد دارد،
-- وقتی تراپیست تاریخِ دقیقِ پرونده‌ی قدیمی را نمی‌داند (تصمیمِ مالک). جلسه‌ی زنده همچنان
-- همیشه تاریخِ واقعی می‌گیرد (سرور fallback به وقتِ ایران) — این ستون فقط اجازه‌ی NULL
-- را می‌دهد، هیچ ردیفِ موجودی تغییر نمی‌کند (additive، LAW-007).
ALTER TABLE sessions ALTER COLUMN date DROP NOT NULL;
