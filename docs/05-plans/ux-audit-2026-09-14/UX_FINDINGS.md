# UX Findings — فیلیا (2026-09-14)

> **وضعیت:** یافته‌ها = **EVIDENCE** (مشاهده در 2026-09-14، LAW-019) · پیشنهادها = **PROPOSED** (بدونِ تأییدِ مالک اجرا نمی‌شوند).
> **کدِ بررسی‌شده:** working tree شاخه‌ی `feat/clarity` روی HEAD `2763414` + فیکس‌های commitنشده‌ی فازِ ۰ (`public/index.html`، `server/src/http/auth.ts`). **Production:** `8bcdf0e`.
> **کارت‌های کامل** (مشاهده، سناریو، پیامد، شواهد، راه‌حل، اعتبارسنجی): [UX_AUDIT_REPORT.md §5](UX_AUDIT_REPORT.md#۵-ux-findings).
> **رابطه با audit قبلی:** ستونِ «مرتبط» به شناسه‌های [ui-ux-audit-2026-09-14](../ui-ux-audit-2026-09-14.md) (UI-xx) اشاره می‌کند. یافته‌ی UX مسئله را از دیدِ تجربه‌ی کاربر قاب می‌گیرد و موارد را تکرار نمی‌کند.

**برچسبِ شاهد:** `اجرا` = در UIِ واقعی با mock backend بازتولید شد · `کد` = از خواندنِ کد · `محاسبه` · `INFERRED` = استنباط، اجرا نشد.
**Prod:** ✅ در production هم هست · WT = فقط working tree · ؟ = بررسی نشد.

## همگام‌سازی با commitهای هم‌زمان (HEAD `8347fbb`)

> این audit روی snapshotِ «`2763414` + فیکس‌های commitنشده‌ی فازِ ۰» انجام شد (همه‌ی **شماره‌خط‌ها و اجراها** مربوط به همان snapshot‌اند). هم‌زمان، نشستِ دیگری سه commit ساخت: `ecf00b4` (UI-01/02/03/04/06/07/10/12/16/17/18/19/21/37)، `fedeac2` (UI-08/11/20-نیمه/27)، `8347fbb` (UI-22/23/24 — نوارِ ثابتِ پایینِ موبایل). هر یافته دوباره با `grep` روی HEAD `8347fbb` بررسی شد (اجرای دوباره انجام نشد). production (`8bcdf0e`) تغییری نکرده است.

| وضعیت در HEAD | یافته‌ها |
|---|---|
| **پابرجا** (شواهدِ کد در HEAD دوباره پیدا شد) | UX-001 (`index.html:3231`، `3283`)، UX-002 (`2409`)، UX-003 (`1439-1444`)، UX-004 (`521`، `660`)، UX-005 (`1490`)، UX-006، UX-007، UX-009 (`591`)، UX-010 (`3781-3795`)، UX-011، UX-012، UX-013، UX-014، UX-015، UX-016، UX-017، UX-018، UX-019 (`697`)، UX-020، UX-021، UX-023، UX-024 (`725`)، UX-025، UX-026، UX-027، UX-030، UX-031، UX-033، UX-034، UX-036 (`3514`)، UX-038، UX-039، UX-040، UX-041، UX-042 |
| **جزئی رفع‌شده** | **UX-008** — کلیک روی جلسه‌ی «در جریان» در پرونده اکنون ادامه می‌دهد (UI-12)؛ باقی: وضعیتِ سروریِ `recovered` برای مسیرِ اصلی، آرشیوِ صدای صف به‌جای رونویسی (INFERRED)، نبودِ برچسب · **UX-022** — یادداشتِ سریع قابلِ حذف شد (UI-37)؛ باقی: ویرایش و افزودن پس از تکمیل · **UX-028** — «مراجع جدید» در موبایل ثابتِ پایینِ صفحه شد (`8347fbb`)؛ باقی: اولین کارت همچنان پس از هدر/تب/جست‌وجو/فیلتر (INFERRED از diff، اندازه‌گیریِ دوباره نشد) · **UX-029** — متن در دید قرار می‌گیرد و پس از ویرایشِ تاریخ باز می‌ماند (UI-10، UI-11)؛ باقی: پیش‌نمایش و اطلاعاتِ پرونده · **UX-032** — Enter در فرمِ ورود کار می‌کند (UI-20 نیمه)؛ باقی: onboarding، بازیابیِ رمز · **UX-035** — متنِ زنده بلافاصله نمایش داده می‌شود (UI-16)؛ باقی: توقفِ تایمر و نوارِ صدا (UI-14، UI-15) · **UX-037** — بنر بالای مودال و Escape/پس‌زمینه (UI-07، UI-21)؛ باقی: بنرِ ماندگار و دور از محلِ اقدام، busy فقط روی ۲ دکمه |
| **تغییرِ زمینه** | **UX-009** — در موبایل «توقف» و «پایان» اکنون در یک نوارِ ثابتِ مشترک (۱۰px فاصله) هستند؛ همچنان بدونِ تأیید |
| **رفع‌شده** | — (هیچ یافته‌ی UX-xx به‌طورِ کامل رفع نشده است) |

## خلاصه

| Severity | تعداد |
|---|---|
| Critical | 5 |
| High | 14 |
| Medium | 18 |
| Low | 5 |
| **جمع** | **42** |

## جدول

| ID | Title | Severity | Confidence | Area | Evidence | Recommendation | Prod | مرتبط |
|---|---|---|---|---|---|---|---|---|
| UX-001 | یادداشتِ سریع، علامت و یادداشتِ صوتی در صورتِ شکستِ ذخیره بی‌صدا از دست می‌روند ولی «ثبت‌شده» نمایش داده می‌شوند | Critical | High | جلسه‌ی زنده، تکمیل | اجرا: با شکستِ `POST /notes` → UI ۱ یادداشت و ۱ علامت، سرور ۰، بدونِ هشدار؛ پس از «ذخیره و پایان» هم ۰ · کد: `addQuickNote` (`index.html:3153-3166`)، `.sign-chip` (`3101-3115`)، `stopVoiceNoteDirect` (`2842-2845`) همه `catch(e){}` | صفِ محلیِ یادداشت (IndexedDB) با retry، نشانِ وضعیت روی هر آیتم (در حالِ ذخیره / ذخیره شد / ذخیره نشد — تلاشِ دوباره)، مسدودکردنِ «ذخیره و پایان» تا خالی‌شدنِ صف | ✅ | UI-02 |
| UX-002 | متنِ یادداشت‌ها، علائم و یادداشت‌های صوتی در پرونده‌ی مراجع نمایش داده نمی‌شود؛ فقط زمان دیده می‌شود | Critical | High | پرونده‌ی مراجع | اجرا: ۳ آیتم → متن‌ها «— ۰۱:۰۰»، «۰۲:۰۰»، بدونِ متنِ یادداشت/نامِ علامت · کد: `viewTranscript` (`2311-2336`) با `span:first-child` که چون اولین فرزند `<svg>` است هرگز match نمی‌شود؛ از commit `f58bd29` | پرکردنِ مستقیمِ spanِ متن (یا ساختِ DOM با `createElement`)؛ نمایشِ یادداشت‌ها در جای زمانیِ خودشان کنارِ متن | ✅ | — |
| UX-003 | در حالتِ «ضبطِ محلی» (رونویسیِ زنده ناموفق/قطع) خروج از حساب مسدود نیست و میکروفون روشن می‌ماند؛ بستنِ تب هم هشدار نمی‌دهد | Critical | High (FAILED اجرا شد؛ RECONNECTING/NETWORK_PAUSED از کد) | هدر، جلسه‌ی زنده | اجرا: `state=FAILED`، `hasActiveRecording()=false` → `logout()` → صفحه‌ی ورود، `micLive=1` · کد: `hasActiveRecording` (`1385-1393`) و `beforeunload` (`3675-3687`) فقط به WSِ باز نگاه می‌کنند؛ `hasOpenConnection` (`feelia-rt.js:1261`) | سیگنالِ «جلسه‌ی فعال» بر اساسِ `currentSession` + وضعیتِ موتور (هر state به‌جز IDLE/COMPLETED/CANCELED)، نه اتصال؛ پنهان‌کردنِ «خروج» و «پنلِ ادمین» در Setup/Live/Wrapup | ✅ (در prod حتی فیکسِ جزئیِ UI-01 هم نیست) | UI-01 |
| UX-004 | متنِ رضایتِ مراجع نادرست و ناقص است: ذخیره‌ی صدا، پردازشِ شخصِ ثالث (Soniox)، آرشیوِ ۱۴روزه و دسترسیِ ادمین گفته نمی‌شود | Critical | High | Setup، پانویسِ همه‌ی صفحات | کد: `#consentBox` (`479-493`)، `.privacy-note` (`614-618`) در برابرِ IndexedDB (`feelia-rt.js:114`)، آرشیو و پخشِ ادمین (`index.html:1680-1739`) | بازنویسیِ متن مطابقِ رفتارِ واقعی (پس از تصمیمِ P0-1)؛ متنِ قابلِ خواندن برای مراجع؛ حذفِ ادعای مطلق از پانویس | ✅ | UI-05، R1، C1 |
| UX-005 | خطای شبکه هنگامِ بازکردنِ اپ به‌صورتِ «هنوز مراجعی ثبت نشده» یا صفحه‌ی ورود نمایش داده می‌شود | Critical | High | ورود، فهرستِ مراجعین | کد: `enterApp` → `api('/api/clients').catch(()=>({clients:[]}))` (`1436`) → `#emptyFirst`؛ `init` هر خطای `/api/auth/me` را «واردنشده» تفسیر می‌کند (`1410-1418`) | حالتِ خطای صریح («اتصال برقرار نشد — تلاشِ دوباره») متمایز از خالی و از خروج؛ تفکیکِ 401 از خطای شبکه | ✅ | UI-33 |
| UX-006 | جلسه‌ای بدونِ رونویسی (مراجعِ ناموافق یا میکروفونِ خراب) در محصول ممکن نیست؛ حتی یادداشت و علامت | High | High | Setup | کد: `declineConsentAndExit` (`1337-1342`) به فهرست برمی‌گردد؛ دکمه‌ی شروع به `consent && preflightMicOk` وابسته است (`1324-1328`) | حالتِ «جلسه بدونِ رونویسی» (فقط یادداشت/علامت/مدت)؛ پیشنهادِ آن روی «موافق نیست» و روی خطای میکروفون | ✅ | UI-36 |
| UX-007 | وضعیتِ «متن هنوز آماده نیست / ناقص / ناموفق» در پرونده دیده نمی‌شود؛ جلسه «کامل» برچسب می‌خورد | High | High | پرونده، تکمیل | اجرا: جلسه با `stt_mode=batch-pending` → برچسبِ «کامل»، بدونِ هیچ نشانه · کد: `openClientDetail` (`2280`) و `viewTranscript` از `stt_mode`/`realtime_reliable` استفاده نمی‌کنند؛ اعلانِ آماده‌شدن فقط بنرِ گذرا (`2757-2763`) | برچسبِ وضعیتِ متن روی هر جلسه («در حالِ آماده‌سازی»، «ناقص — بخشی از صدا رونویسی نشد»، «کامل») و در بالای متن | ✅ (جزئی) | — |
| UX-008 | پس از بستنِ تب/کرش، جلسه برای همیشه «در جریان» می‌ماند، فقط از همان مرورگر قابلِ ادامه است و صدای صف‌شده آرشیو می‌شود نه رونویسی | High | Medium | پرونده، بازیابی | کد: `recovered` فقط در مسیرِ legacy (`server/src/ws/transcription.ts:158,316`)؛ ادامه فقط با `localStorage` (`1762-1783`)؛ `sweepOrphanedAudioQueue` با `purpose=archive` (`1466`)؛ drain در جلسه‌ی ادامه‌یافته `purpose` را از `unreliable`ِ نشستِ تازه می‌گیرد (`feelia-rt.js:1130`) — INFERRED | دکمه‌ی «ادامه / تکمیل» روی هر جلسه‌ی «در جریان» در پرونده؛ علامت‌گذاریِ سروری؛ رونویسیِ صدای صف‌شده‌ی جلسه‌های ناقص و اطلاع به کاربر | ✅ | UI-12، UI-35 |
| UX-009 | «پایان جلسه» با یک لمس، بدونِ تأیید و بدونِ امکانِ بازگشت اجرا می‌شود | High | High | جلسه‌ی زنده | کد: `#btnEndSession onclick="endSession()"` (`551`)؛ `endNewRTSession` بلافاصله `status=completed` (`2753-2755`) | تأییدِ سبک (نگه‌داشتن یا دومرحله‌ای با ۳ ثانیه واگرد)؛ فاصله از «توقف موقت» | ✅ | UI-22 |
| UX-010 | صفحه‌ی تکمیل متنِ جلسه را نشان نمی‌دهد و پس از «ذخیره و پایان» کاربر به فهرستِ مراجعین (نه پرونده) می‌رود | High | High | تکمیل | اجرا: `transcriptVisibleInWrapup=false`؛ پس از `finishSession` صفحه = `Clients` · کد: `#screenWrapup` (`556-593`)، `finishSession` (`3638-3655`) | پیش‌نمایشِ متن در تکمیل؛ پس از پایان، رفتن به همان جلسه در پرونده با پیامِ «ذخیره شد» | ✅ | UI-19 |
| UX-011 | مفهومِ «گزارشِ جلسه» در محصول وجود ندارد؛ خروجی فقط متنِ خامِ گوینده‌دار است | High | High | معماریِ اطلاعات | کد: هیچ مسیرِ گزارش/خلاصه (Master Reference §3)؛ نمونه‌ی اولیه `session_assistant_v11 (3).html` خروجی را «گزارش» می‌نامید | تصمیمِ محصول: تعریفِ «گزارش» (قالبِ یادداشتِ جلسه با بخش‌های متن/علائم/یادداشت‌ها) یا حذفِ این انتظار از زبانِ محصول | ✅ | — |
| UX-012 | متنِ جلسه فقط‌خواندنی است: اصلاحِ خطای رونویسی، نام‌گذاریِ گوینده (تراپیست/مراجع) و جست‌وجو ممکن نیست | High | High | پرونده | اجرا: `transcriptBox.isContentEditable=false`؛ برچسب‌ها «گوینده ۱/۲» (`feelia-rt.js:585`) | ویرایشِ متن با CAS و تاریخچه؛ نگاشتِ «گوینده N → تراپیست/مراجع»؛ جست‌وجو در متن | ✅ | — |
| UX-013 | خطرِ ثبتِ جلسه برای مراجعِ اشتباه و نبودِ امکانِ اصلاح (انتقالِ جلسه) | High | Medium | فهرست، Setup | کد: شناسه فقط کدِ `CL-XXXX` + نام مستعارِ اختیاری (`2079-2091`)؛ نام مستعار با `—` برای خالی؛ Setup نام را در متنِ ۱۳px نشان می‌دهد (`476`)؛ endpointِ جابه‌جاییِ جلسه وجود ندارد | نامِ مراجع بزرگ و تأییدی در Setup و Live؛ هشدارِ نام‌های مشابه؛ «انتقالِ جلسه به مراجعِ دیگر» | ✅ | UI-38 |
| UX-014 | تاریخ و ساعتِ جلسه ناهمگون و بالقوه غلط: Setup شمسی، رکورد میلادی، ساعت از ساعتِ سرور | High | High (تقویم) / Low (منطقه‌ی زمانی — INFERRED) | Setup، Live، پرونده | اجرا: Setup «۱۴۰۵/۰۶/۲۳» ↔ Live «۲۰۲۶/۰۹/۱۴» · کد: `server/src/http/sessions.ts:73-75` (`new Date()` سرور)؛ راهنمای ویرایش شمسی (`714`) | ذخیره‌ی ISO از کلاینت + نمایشِ شمسی یکسان؛ date-picker شمسی | ✅ | UI-09، C4 |
| UX-015 | واژگانِ «رونویسی / ضبط / ذخیره» متناقض است: رضایت می‌گوید صدا ذخیره نمی‌شود، صفحه‌ی جلسه می‌گوید «صدا در حالِ ضبط است» | High | High | Setup، Live، تکمیل | کد: `2662-2663`، `2708-2711`، `579` («پایان ضبط و ثبت») در برابرِ `486`، `616` | واژه‌نامه‌ی یکسان و صادق؛ وضعیت‌ها با همان واژه‌های متنِ رضایت | ✅ | UI-05 |
| UX-016 | تراپیست نمی‌داند ادمینِ پلتفرم می‌تواند صدای جلسه را بشنود و همه‌ی متن‌ها را خروجی بگیرد | High | High | اعتماد | کد: `openAdminClientSessions` (`1680-1739`)، `adminDownloadTherapist/All` (`1754-1759`)؛ هیچ متنی در UIِ تراپیست | صفحه‌ی «داده‌های شما کجاست و چه کسی می‌بیند» + ذکر در ثبت‌نام | ✅ (پخشِ صدا فقط WT) | R1 |
| UX-017 | روی دستگاهِ مشترکِ مطب: نشستِ ۳۰روزه، بدونِ قفل پس از بی‌کاری؛ فهرستِ مراجعین بلافاصله با بازکردن نمایش داده می‌شود | High | Medium | حریمِ خصوصی | کد: کوکی ۳۰ روز (REQ-006)؛ `init` مستقیم `enterApp` | قفلِ بی‌کاری (مثلاً ۱۵ دقیقه) با رمز/PIN؛ گزینه‌ی «این دستگاه مشترک است» | ✅ | R7 |
| UX-018 | کیبورد و صفحه‌خوان: جلسه‌ها و کارت‌ها فوکوس نمی‌گیرند؛ اولین Tab در پرونده روی «حذفِ جلسه» است؛ ۲۰ دکمه‌ی هم‌نامِ «شروع جلسه» | High | High | دسترسی‌پذیری | اجرا: `tabIndex=-1` برای `.session-item` و `.client-card`؛ Tab → `button.session-delete`؛ درختِ دسترسی: «شروع جلسه» ×N بدونِ نامِ مراجع، دکمه‌ی تم بدونِ نام | `<button>`/`<a>` برای ردیف‌ها؛ `aria-label` شاملِ نامِ مراجع؛ `aria-live` برای وضعیتِ جلسه؛ `<label for>` | ✅ | UI-39 |
| UX-019 | دلیلِ غیرفعال‌سازیِ مراجع به‌طورِ پیش‌فرض «ناتوانیِ مالی» انتخاب‌شده است | High | High | فهرستِ مراجعین | کد: `value="ناتوانی مالی" checked` (`653`)؛ `openDeactivateModal` اولین گزینه را تیک می‌زند (`2151-2152`) | بدونِ پیش‌فرض، یا پیش‌فرضِ خنثی «بدونِ دلیل» | WT | — |
| UX-020 | صفحه‌ی جلسه‌ی زنده بارِ شناختیِ بالا دارد: ۸ پیامِ وضعیتِ فنی، دو کانالِ بنر، وضعیت با متنِ کوچک و کم‌کنتراست | Medium | Medium | جلسه‌ی زنده | کد: `rtOnState` (`2703-2734`)، `setLiveConnBanner` (`1260`)، `.rec-status` 13.5px `--muted` | یک نشانگرِ سه‌حالته (سبز: همه‌چیز خوب / زرد: متن بعداً آماده می‌شود / قرمز: اقدام لازم) با یک جمله‌ی کوتاه | ✅ | UI-14، UI-15 |
| UX-021 | علائم: عنوانِ «علائم بدنی» با محتوا نمی‌خواند؛ برچسبِ چیپ با مقدارِ ذخیره‌شده فرق دارد؛ فهرست ثابت است | Medium | High | جلسه‌ی زنده | کد: `520-535` («تنش»→«تنش عضلانی»، «سکوت»→«سکوت طولانی»، «پرخاش»→«پرخاشگری»، «گریز چشمی»→«اتصال چشمی گریزان») | برچسبِ یکسان در ثبت و نمایش؛ عنوانِ «علائم و رفتارها»؛ فهرستِ قابلِ شخصی‌سازی | ✅ | — |
| UX-022 | پس از خروج از صفحه‌ی تکمیل، افزودن یا اصلاحِ یادداشت برای آن جلسه ممکن نیست؛ یادداشتِ حینِ جلسه هم قابلِ اصلاح نیست | Medium | High | پرونده | کد: `#sessionDetail` (`601-609`) فقط «ویرایش تاریخ/ساعت» و «بازسازی گوینده‌ها»؛ `renderNotesLog` بدونِ حذف/ویرایش (`3174-3190`) | افزودن/ویرایش/حذفِ یادداشت در پرونده | ✅ | UI-37 |
| UX-023 | حذفِ یادداشت (متنی/صوتی) در تکمیل و حذفِ علامت در جلسه با یک لمس، بدونِ تأیید یا واگرد | Medium | High | تکمیل، جلسه | کد: `.note-remove` (`3503-3511`)، `.sign-rm` 11px با `opacity:.55` (`231-233`، `3139-3147`) | واگردِ ۵ثانیه‌ای («حذف شد — برگردان») | ✅ | — |
| UX-024 | «لغو جلسه» یادداشت‌ها و علائم را هم حذف می‌کند ولی مودال فقط از «متن» می‌گوید؛ دکمه درست زیرِ «پایان جلسه» است | Medium | High | جلسه‌ی زنده | کد: مودال (`681`)؛ `confirmCancelSession` → `DELETE /api/sessions/:id` (`3465`) | متنِ دقیق («متن، N علامت و N یادداشت حذف می‌شود»)؛ جداکردنِ مکانیِ «لغو» | ✅ | — |
| UX-025 | «جایگزینیِ متن» پس از بازسازیِ گوینده‌ها برگشت‌ناپذیر است و نسخه‌ی قبلی نگهداری نمی‌شود | Medium | Medium | پرونده | کد: `applyResolvedSpeakers` (`2389-2408`) کلِ `transcript` را جایگزین می‌کند؛ مودال (`639`) | نگهداریِ نسخه‌ی قبلی و «بازگشت به نسخه‌ی قبل»؛ نمایشِ تفاوت | WT | UI-25 |
| UX-026 | کار با چند تب/دستگاه قفل ندارد: یک جلسه در دو تب ادامه یا دو جلسه هم‌زمان ضبط می‌شود | Medium | Medium | جلسه | کد: کلیدِ مشترکِ `feelia_active_session` (`2546`)؛ هیچ lock سمتِ سرور/کلاینت — INFERRED | قفلِ «این جلسه در تب/دستگاهِ دیگری باز است» | ✅ | — |
| UX-027 | دکمه‌ی Back و Refreshِ مرورگر: بدونِ مسیریابی؛ Back از اپ خارج می‌شود و Refresh کاربر را به فهرستِ مراجعین برمی‌گرداند | Medium | High | ناوبری | اجرا: `history.length` پس از چند تغییرِ صفحه ثابت ماند · کد: `showScreen` (`1293-1303`) بدونِ `pushState` | `history.pushState` برای صفحه‌ها (بدونِ داده‌ی حساس در URL — LAW-001) | ✅ | — |
| UX-028 | فهرستِ مراجعین در موبایل: اولین کارت در ۵۸۳px و «مراجع جدید» در ۴۰۱۴px (۲۰ مراجع) | Medium | High | موبایل | اجرا: viewport ۳۷۵×۸۱۲، `firstCardTop=583`، `newClientTop=4014`، `docH=4170`، ارتفاعِ کارت ۱۶۰ | دکمه‌ی «مراجع جدید» بالا/شناور؛ کارتِ فشرده؛ فیلترها جمع‌شونده | ✅ | UI-24 |
| UX-029 | «پرونده» فقط فهرستِ جلسات است: بدونِ خلاصه، پیش‌نمایشِ جلسه، شمارِ یادداشت، یا اطلاعاتِ مراجع | Medium | Medium | معماریِ اطلاعات | کد: `openClientDetail` (`2253-2298`)؛ ردیف فقط «جلسه N · وضعیت · تاریخ · مدت» | پیش‌نمایشِ خطِ اول/یادداشت‌ها روی ردیف؛ بخشِ «یادداشتِ پرونده» (منوط به تحقیق — Q-07) | ✅ | UI-10 |
| UX-030 | پیام‌های خطا عمومی یا فنی‌اند و اقدامِ بعدی را نمی‌گویند | Medium | High | همه | کد: «خطا در ثبت» (`3534`)، «خطا: » + پیامِ خام (`1897`)، «توقف اعمال نشد — وضعیت فعلی اجازه توقف نمی‌دهد» (`2788`)، «نسخه‌ی transcript قدیمی است» (`server sessions.ts:155`) | قالبِ «چه شد + چه چیزی امن است + چه کنم» | ✅ | UI-33 |
| UX-031 | رضایت با یک لمسِ تراپیست ثبت می‌شود؛ زمان و متنِ رضایت در پرونده دیده نمی‌شود | Medium | High | Setup، پرونده | کد: `setConsent` (`1329-1336`) فقط `consent:true` می‌فرستد؛ پرونده هیچ نشانی از رضایت ندارد | ثبت و نمایشِ «رضایت: بله، ساعتِ …، نسخه‌ی متن» روی جلسه | ✅ | UI-36 |
| UX-032 | اولین تجربه: صفحه‌ی ورود ارزشِ محصول را توضیح نمی‌دهد؛ بازیابیِ رمز و راهنمای «اولین جلسه» ندارد | Medium | High | ورود، شروع | کد: `#screenAuth` (`366-389`)؛ Master Reference §21 (بدونِ بازیابیِ رمز) | صفحه‌ی ورودِ کوتاه با سه جمله‌ی ارزش و حریمِ خصوصی؛ «فراموشیِ رمز»؛ checklistِ شروع | ✅ | UI-20، UI-45، UI-47 |
| UX-033 | حذفِ مراجع/جلسه «برای همیشه» اعلام می‌شود ولی فایل‌های صدای آرشیو ممکن است روی سرور بمانند | Medium | Low | اعتماد | کد: `showDeleteClient` (`2217-2221`)؛ R4 (INFERRED) | هم‌راستاکردنِ رفتار با متن (P0-3) | WT | R4، REQ-093 |
| UX-034 | کنتراستِ متن‌های کمکی زیرِ ۴٫۵:۱ | Medium | High | دسترسی‌پذیری | محاسبه (اجرا): `--muted` روی کارت ۳٫۲۸، روی پس‌زمینه ۲٫۹۹؛ سفید روی `--clay` ۳٫۸۱ | تیره‌کردنِ `--muted` تا ≥ ۴٫۵ | ✅ | UI-40 |
| UX-035 | بازخوردِ «آیا کار می‌کند؟» در جلسه ضعیف است: متنِ زنده پیش‌فرض پنهان و هنگامِ روشن‌کردن خالی؛ نوارِ صدا و تایمر هنگامِ قطعی می‌ایستند | Medium | High | جلسه‌ی زنده | اجرای audit قبلی: متنِ زنده ۰ کاراکتر در برابرِ ۸۹؛ `timerRunning=false` در RECONNECTING | نمایشِ فوریِ متنِ موجود؛ ادامه‌ی تایمر و نوارِ صدا تا وقتی میکروفون ضبط می‌کند | ✅ | UI-14، UI-15، UI-16 |
| UX-036 | مدتِ جلسه ناهمگون نمایش داده می‌شود («۰ دقیقه»؛ `floor` در تکمیل و `round` در پرونده) | Medium | High | تکمیل، پرونده | اجرا: «مدت: ۰ دقیقه» · کد: `3378` (`Math.floor`) در برابرِ `2290` (`Math.round`) | قالبِ «۴۵ دقیقه» / «کمتر از ۱ دقیقه» با یک تابع | ✅ | UI-34 |
| UX-037 | اعلان‌ها بالای صفحه و خارج از دید، ماندگار و زیرِ مودال؛ نشانگرِ «در حالِ انجام» فقط روی دو دکمه | Medium | High | همه | کد: `showBanner` (`855`)؛ `disabled` فقط در `createNewClient`/`startSession` | toast نزدیکِ محلِ اقدام؛ الگوی مشترکِ busy | ✅ (جزئی) | UI-06، UI-07، UI-32 |
| UX-038 | لحنِ محاوره‌ای و واژه‌های فنی کنارِ لحنِ رسمی («سگمنت»، «نهایی‌سازی»، «می‌کنه»، «هستی») | Low | High | محتوا | کد: `639`، `945`، `1714`، `2362` | راهنمای لحن و واژه‌نامه (بخشِ Persian Content UX) | ✅ (جزئی) | — |
| UX-039 | انیمیشنِ تنفسِ همیشگی بدونِ پشتیبانی از `prefers-reduced-motion` | Low | High | دسترسی‌پذیری | اجرا: هیچ قاعده‌ی `reduced-motion` در CSS | غیرفعال‌کردنِ انیمیشن با این تنظیم | ✅ | — |
| UX-040 | تمِ تیره از تنظیمِ سیستم پیروی نمی‌کند و ذخیره نمی‌شود (برخلافِ design system) | Low | High | بصری | کد: `toggleTheme` (`1282-1285`)؛ `feelia-design-system.html` از سیستم پیروی می‌کند | پیروی از سیستم + ذخیره‌ی انتخاب | ✅ | UI-26 |
| UX-041 | فونت از Google Fonts لود می‌شود | Low | Low | کارایی | کد: `<link>` (`8`) — اثر در ایران INFERRED | فونتِ محلی | ✅ | UI-43 |
| UX-042 | نمادِ مرکزیِ جلسه (قلم روی دایره‌ی قرمزِ تپنده) معنای «ضبط» و «نوشتن» را مخلوط می‌کند | Low | Low | بصری | کد: `.rec-dot` (`199-205`، `510-514`) — برداشتِ کاربر ASSUMPTION | آزمونِ ۵ثانیه‌ای با کاربر (Q-03) | ✅ | — |
