# رضایتِ یک‌باره برایِ هر مراجع (2026-09-25)

> **Evidence** (LAW-019). دستورِ مالک: «وقتی یه بار پیام رضایت میگیره … طوری باشه که برای هر مراجع فقط یه بار رضایت بده».

## تغییر
- **Migration 024** (`024_client_recording_consent.sql`): `clients.recording_consent_at DATETIME NULL` — افزودنی و بدونِ backfill.
- **سرور:**
  - `http/clientConsent.ts` (جدید) با `hasStoredConsent` و `recordClientConsent`؛ فقط اولین بار ثبت می‌شود و رویدادِ `client.consent_recorded` می‌سازد.
  - `POST /api/sessions` (زنده) و `POST /api/uploads`: رضایت = `consent:true` یا رضایتِ ثبت‌شده. `consent:true` ثبتش می‌کند. بدونِ هر دو ⇒ `400 consent-required`.
  - `DELETE /api/clients/:id/recording-consent`: لغو، با رویدادِ `client.consent_revoked`.
  - `GET /api/clients` فیلدِ `recording_consent_at` را برمی‌گرداند (جزئیات با `SELECT *` از قبل برمی‌گرداند).
- **UI (`index.html`):**
  - صفحه‌ی شروعِ جلسه: اگر رضایت ثبت شده، جعبه‌ی رضایت نمایش داده نمی‌شود و به‌جایش خطِ «رضایتِ این مراجع … از <تاریخ> ثبت شده است» با دکمه‌ی «مراجع رضایتش را پس گرفته» می‌آید.
  - مودالِ آپلود: همین منطق برایِ تیک.
  - زیرِ جعبه‌ی رضایت یک خطِ توضیحیِ جدا اضافه شد. **خودِ متنِ رضایت دست نخورد** (دستورِ قبلیِ مالک).
- **LAW-009** برقرار است و فقط دامنه‌اش مستند شد. جلسه‌ی دستی (بدونِ ضبط) مثلِ قبل بدونِ رضایت مجاز است.

## تست
- **E2E رویِ MySQLِ dev** (fixtureِ canary، migration 024 رویِ dev اعمال شد): **11/11 PASS**، fixtures remaining 0. سناریوها:
  - مراجعِ تازه 400 می‌گیرد. اولین رضایت 201 است و ثبت می‌شود. جلسه‌ی بعد بدونِ consent 201 است و زمانِ اولین رضایت ثابت می‌ماند.
  - رضایت به مراجعِ دیگر سرایت نمی‌کند. فیلد در `GET /api/clients` هست.
  - تراپیستِ دیگر ⇒ 404. لغو ⇒ جلسه‌ی بعد 400 و جلسه‌هایِ قبلی دست‌نخورده.
  - آپلود: 400 ⇒ ثبت ⇒ 201. رضایتِ آپلود برایِ جلسه‌ی زنده هم معتبر است.
  - جلسه‌ی دستی بدونِ رضایت 201 می‌گیرد و رضایتی ثبت نمی‌کند.
  - رویدادهایِ حسابرسی در `obs_events` ثبت شدند.
- ردیف‌هایِ canary در `obs_events` عمداً ماندند: بدونِ FK به‌طراحی، با پاک‌سازیِ سنی.
- **UI (Browser pane با mock):**
  - مراجعِ دارایِ رضایت ⇒ جعبه پنهان، خطِ «ثبت شده از ۱۴۰۵/۰۶/۲۹»، `consent=true`.
  - لغو با دکمه‌ی واقعی و تأیید ⇒ `DELETE …/recording-consent` ارسال شد، جعبه برگشت، دکمه‌ی شروع قفل شد.
  - مودالِ آپلود: مراجعِ دارایِ رضایت ⇒ تیک پنهان و دکمه فعال. مراجعِ بدونِ رضایت ⇒ تیک لازم. بعد از آپلود ⇒ بازکردنِ دوباره دیگر نمی‌پرسد.
  - مراجعِ بدونِ رضایت ⇒ جعبه با متنِ دست‌نخورده، خطِ توضیحیِ جدید، و «موافق است» کار می‌کند.
  - اسکرین‌شات ممکن نشد (پنجره minimize بود)؛ همه‌چیز با DOM تأیید شد.
- `test:rt` 55/0، `test:cf` 108/108، `test:up` 31/31، `tsc` تمیز، build (migration 024 در dist).

## deploy (دستورِ مالک: «انجامش بده، deploy کن»)
- پیش از آن: در ۳ ساعتِ گذشته جلسه‌ی زنده، آپلود یا jobِ فعال نبود. آخرین migration ۰۲۳ بود.
- **backup:** DB در `/root/backups/feelia-pre-024-2026-09-24T20-44-31-542Z.sql` (794KB، «Dump completed»)؛ کد در `/root/backups/code-before-consent-20260924T204510Z.tar.gz`.
- deploy: tar بدونِ `.env`/`data`/`node_modules` ⇒ `pnpm install` ⇒ `pm2 restart` ⇒ **`024_client_recording_consent.sql applied`**؛ health ok.
- **smoke:**
  - ستونِ `recording_consent_at` (datetime، nullable) ساخته شد. ۳۰ مراجع و ۴۱ جلسه دست‌نخورده‌اند.
  - برایِ ۰ مراجع رضایت ثبت شده (بدونِ backfill، مطابقِ انتظار).
  - `index.html`ِ سروشده UIِ رضایت را دارد. `dist/http/clientConsent.js` موجود است. `DELETE …/recording-consent` بدونِ نشست 401 می‌دهد.
  - پروسه online است (restarts=8، ۱۰۸MB).
- **rollback (در صورتِ نیاز):** کدِ قبلی از backup. ستونِ 024 بی‌ضرر است و می‌ماند (کدِ قبلی آن را نمی‌خواند).
