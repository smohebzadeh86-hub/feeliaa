# Verification — ارجاعِ متقاطع + قبل/اکنونِ ردیفی + جابه‌جاییِ یافته + ارتقایِ پرونده‌یِ قدیمی — 2026-09-20/22

> Evidence (مشاهده در یک زمان). دادهٔ کاملاً ساختگی (LAW-001). commit نشده.

| بررسی | نتیجه |
|---|---|
| `tsc --noEmit` | تمیز |
| `pnpm test:cf` | 100 PASS / 0 FAIL (O1–O4، P1–P7، Q1–Q7، R1–R8 جدید) |
| mutation: خاموش‌کردنِ `refs` | O1، O3، O4، P4 FAIL؛ برگردانده شد |
| mutation: خاموش‌کردنِ خانه‌یِ ردیف (change) | P4 FAIL؛ برگردانده شد |
| mutation: سست‌کردنِ نگهبانِ ارتقا (`upgradable`) | Q3 FAIL؛ برگردانده شد |
| mutation: خاموش‌کردنِ `reapplyMoves` در merge | R6 FAIL؛ برگردانده شد |
| `pnpm test:rt` | 35 PASS / 0 FAIL |
| syntax اسکریپتِ inline (`vm.Script`) | ok |
| UI: پرونده‌ی قدیمی ⇒ ارتقا | دکمه دیده شد؛ بعدِ کلیک (با override موقتِ `confirm` — پایین را ببین) ۶ بخش ارتقا یافت؛ ستاره‌ها (۵) و پنل‌هایِ انتقال (۳) ظاهر شدند؛ کنسول بدونِ خطایِ جدید |
| UI: ستاره‌گذاری بعدِ ارتقا | PATCH واقعی (`pin`) ⇒ کارتِ نکته‌ی کلیدی ظاهر شد |
| UI: انتقالِ یافته بعدِ ارتقا | PATCH واقعی (`move`) ⇒ پنلِ محور/نقش کار کرد، لاگ ثبت شد |
| UI: ارجاعِ متقاطع (fixtureِ مدل، نه ارتقا) | چیپِ ارجاع + کلیک ⇒ اسکرول/بازشدنِ محور/flash روی یافته‌ی هدف |
| UI: «قبل/اکنون» ردیفی | ۲ ردیف، ردیفِ اول برجسته؛ حالتِ ویرایش به دو جعبه برمی‌گردد + یادداشت |
| `overflowX` | false در همه‌ی موارد |

## باگِ ابزاریِ این نشست (نه در کدِ محصول)
- `window.confirm()` در Browser pane به‌صورتِ خودکار لغو می‌شود (نه دیالوگِ واقعی) — برایِ آزمودنِ مسیرِ «ارتقا» موقتاً override شد؛ رفتارِ خودِ `cfUpgrade()` (که `confirm` واقعی صدا می‌زند) تغییر نکرد.
- چند بار در طولِ نشست Browser pane/mock بی‌صدا بسته شدند (از جمله بینِ دو پیامِ کاربر)؛ هر بار با راه‌اندازیِ دوباره رفع شد. زمان‌بندیِ آن مستقل از کدِ محصول است.

## محدودیت‌ها (صادقانه)
- تست با DB/سرورِ واقعی و حسابِ واقعی انجام نشده (ممنوع بدونِ اجازه‌ی صریح، LAW §۶).
- ارتقایِ پرونده‌یِ قدیمی چیزِ یتیم/ارجاعِ متقاطع تولید نمی‌کند (بدونِ `factIds`)؛ عمدی و مستند.
- نقلِ عینی در محورها هنوز فقط با پرامپت کنترل می‌شود، نه کد.
- قاعده‌ی باز/بستنِ نقشه‌ی راه و ابهام‌ها به‌صراحت پیاده نشد.
- مدلِ واقعی برایِ همین سه ویژگی (ارجاع/ردیف/جابه‌جایی/ارتقا) اجرا نشد — فقط fixtureِ ساخته‌شده از کدِ خودِ پروژه و mutation؛ توصیه می‌شود قبل از تکیه‌ی کامل، یک canaryِ واقعیِ دیگر هم گرفته شود.
