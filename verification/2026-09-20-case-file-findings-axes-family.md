# Verification — «یافته» برای محورها و خانواده + «یک فکت = یک خانه» — 2026-09-20

> Evidence (مشاهده در یک زمان). دادهٔ کاملاً ساختگی (LAW-001).

| بررسی | دستور/روش | نتیجه |
|---|---|---|
| typecheck | `cd server && npx tsc --noEmit` | تمیز |
| harness پرونده | `pnpm test:cf` | 46 PASS / 0 FAIL (G1–G12، H1–H7 جدید) |
| mutation: بازیابیِ یتیم | حذفِ شرطِ `referenced` | 10 FAIL؛ برگردانده شد |
| mutation: dedupe | `axes = fin.axes` (بدونِ dedupe) | 5 FAIL (H1، H2، H4، H5، H7)؛ برگردانده شد |
| harness realtime | `pnpm test:rt` | 35 PASS / 0 FAIL |
| مدل واقعی #۱ | OpenRouter `deepseek/deepseek-v4.1-flash`، مرحله‌ی ۲، ۱۳ فکت | schema پذیرفته؛ ۰ شناسه‌ی ساختگی؛ ۰ یتیم؛ **۸ از ۱۵ فکت در >۱ بخش**؛ ۲۶۲ث |
| مدل واقعی #۲ (پرامپتِ اصلاح‌شده) | همان digest | ۱۳/۱۳ فکت ارجاع؛ تکرارِ خامِ مدل **۰ از ۱۶**؛ ۰ یتیم؛ ۵ محورِ متناسب با محتوا؛ **۴۶۶ث** |
| بازپخشِ خروجیِ #۱ از کدِ جدید | `finalizeDraft` روی `raw` ذخیره‌شده | ۸ تکرار حذف؛ ۲ محورِ تکراری حذف؛ ۰ یتیم |
| UI v2/v1 | mock + `public/` واقعی | محور: `summary`، تیترِ نقش‌ها به ترتیب، چیپ‌ها؛ خانواده با items؛ v1 سالم؛ `overflowX=false` |

## محدودیت‌ها
- Browser pane پنهان بود ⇒ **قضاوتِ بصری انجام نشد**؛ فقط DOM/استایل.
- تست با DB/سرورِ واقعی و پرونده‌ی کاملِ end-to-end انجام نشده.
- `dedupeSingleHome` وقتی یافته ارجاع (`factIds`) ندارد نمی‌تواند تکرار را تشخیص دهد.
- نقلِ عینیِ داخلِ محور فقط با ارجاع/متنِ مدل می‌آید؛ عینیتِ کدی فقط در نقشِ `quotes`.
- ⚠️ زمانِ مرحله‌ی ۲: ۲۶۲ / ۲۹۳ (گامِ ۱) / **۴۶۶** ثانیه (سه نمونه). با digest نزدیکِ یا بالاتر از قفلِ ۸ دقیقه‌ایِ generateCaseFile.
