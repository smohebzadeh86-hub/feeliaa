# Verification — زمانِ تولید: heartbeat + سقفِ reasoning — 2026-09-20

> Evidence (مشاهده در یک زمان). دادهٔ کاملاً ساختگی (LAW-001).

| اجرا (مرحله‌ی ۲، همان مدل) | زمان | توکنِ خروجی | reasoning |
|---|---|---|---|
| base (بدونِ تنظیم) | **789ث** | 17٬368 | **14٬132** |
| `reasoning:{effort:low}` | 184ث | 2٬331 | 0 |
| `reasoning:{enabled:false}` | 278ث | 3٬051 | 0 |
| `provider.sort=throughput` | 185ث (Morph) | 2٬851 | 0 |
| `provider.sort=latency` | 215ث (Morph) | 3٬427 | 0 |

| بررسی | نتیجه |
|---|---|
| `pnpm test:cf` | 52 PASS / 0 FAIL (I1–I4، J1–J2 جدید) |
| mutation: حذفِ timerِ heartbeat | I1، I4 FAIL؛ برگردانده شد |
| `tsc --noEmit` | تمیز |
| `pnpm test:rt` | 35 PASS / 0 FAIL |
| مسیرِ کاملِ واقعی (adapterِ پروژه، `low`) | digest 102ث + compose 186ث = **288ث**؛ 11 heartbeat؛ 10/10 فکتِ کاشته‌شده؛ بنرِ ایمنی درست خالی |
| اجرایِ قبلیِ همان مسیر | **ECONNRESET** (قطعِ گذرایِ شبکه) ⇒ `maxRetries` از ۱ به ۲ برگشت |

## محدودیت‌ها
- هر ردیف یک نمونه است؛ واریانس زیاد است (۱۸۴–۷۸۹ث). نتیجه‌گیریِ آماری نمی‌شود کرد.
- اثرِ `low` روی کیفیتِ digest فقط با یک اجرای کامل سنجیده شد.
- تایم‌اوتِ کلاینت 600000ms در بدترین حالت (۲ مرحله × ۱۰ دقیقه) کوتاه‌تر است.
- تست با DB/سرورِ واقعی انجام نشده.
