# Verification — نقش‌هایِ بیشتر + ترتیبِ لحن + باز/بسته بر اساسِ اهمیت — 2026-09-20

> Evidence (مشاهده در یک زمان). دادهٔ کاملاً ساختگی (LAW-001).

| بررسی | نتیجه |
|---|---|
| `tsc --noEmit` | تمیز |
| `pnpm test:cf` | 56 PASS / 0 FAIL (K1–K4 جدید) |
| mutation | حذفِ sort ⇒ K3 FAIL؛ حذفِ یک نقش از UI ⇒ K1 FAIL؛ برگردانده شد |
| `pnpm test:rt` | 35 PASS / 0 FAIL |
| syntax اسکریپتِ inline | ok (`vm.Script`) |
| UI پیش‌فرض (7 محور، 3 جلسه) | 1 حساس + 3 توجه باز؛ توجهِ پنجم و 2 مطلوب بسته؛ فقط جلسه‌ی 3 باز |
| UI تعامل | 2 کلیک ⇒ 2 کلید ذخیره (نه 4)؛ بعد از بازرندر و reload می‌ماند؛ بازکردن/جمع‌کردنِ همه و برچسبش درست |
| UI پرونده‌ی قدیمیِ وارونه | مرتب می‌شود؛ کنسول بدونِ خطا؛ `overflowX=false` |
| مدل واقعی اجرای ۱ | 407ث (digest 345 + compose 62)؛ 10/10؛ **بنرِ ایمنی با «رد‌شدنِ خودکشی» پر شد (خطا)** |
| مدل واقعی اجرای ۲ (بعد از جمله‌ی prompt) | 86ث (33 + 53)؛ 10/10؛ `safetyRisk` خالی؛ نتیجه‌ی منفی در محورِ «ایمنی» |

## محدودیت‌ها
- Browser pane پنهان ⇒ قضاوتِ بصری انجام نشد.
- رفعِ بنرِ ایمنی فقط با prompt است و یک نمونه بعد از رفع؛ چکِ کدی ندارد.
- زمانِ تولید ۸۶–۴۰۷ث در اجراهایِ اخیر؛ واریانس بسته نشده.
- `localStorage` فقط در همان مرورگر؛ روی دستگاهِ دیگر پیش‌فرضِ اهمیت اعمال می‌شود.
