# Verification — آپلودِ فایلِ صوتیِ جلسه + رفعِ F1–F8 (2026-09-23)

> **Evidence** (مشاهده در یک لحظه، LAW-019). commit نشده، deploy نشده. دادهٔ تست‌ها همه ساختگی (بدونِ دادهٔ بالینی).
> مالک برایِ تستِ واقعی فقط «اجرایِ سرورِ dev» را مجاز کرد؛ حسابِ canary، Sonioxِ واقعی و ری‌استارتِ وسطِ پردازش **مجاز نشد** و انجام نشد.

## ۱. خودکار

| فرمان | نتیجه |
|---|---|
| `cd server && npx tsc --noEmit` | تمیز |
| `pnpm test:up` (جدید) | **24 PASS / 0 FAIL** — H1–H16 ماشینِ حالت (happy path، retry بدونِ متنِ تکراری، ری‌استارت وسطِ رونویسی بدونِ آپلودِ دوباره، backoff، شکستِ دائمی بی‌هزینه، too-long، مهلتِ Soniox، ۴۰۴، Invalid audio، خطایِ گذرایِ دریافتِ متن ≠ سکوت، سکوتِ واقعی، پرونده‌ی busy، ری‌استارت بعد از نرمال‌سازی، صدایِ منقضی، jobِ حذف‌شده)، H17–H19 توابعِ خالص، H20–H24 **ffmpegِ واقعی** (probeِ wav/m4a/mp3/ogg/flac/wma/webm/ویدیوی mp4؛ ویدیوی بی‌صدا ⇒ no-audio؛ فایلِ خراب/playlist ⇒ unreadable؛ PDF با پسوندِ mp3 ⇒ رد؛ نرمال‌سازی به Opus/Ogg و کاهشِ حجمِ WAV > ۲۰×) |
| mutation check | با غیرفعال‌کردنِ دو گاردِ ادامه (normalized موجود، fileIdِ ذخیره‌شده) H9 و H14 FAIL شدند؛ فایل بازگردانده شد و دوباره 24/24 |
| `pnpm test:cf` | 108 PASS / 0 FAIL (بدونِ رگرسیون از تغییرِ CASِ پرونده) |
| `node --check` رویِ `feelia-upload.js` و اسکریپتِ inlineِ `index.html` | OK |
| `pnpm test:rt` | اجرا نشد — `feelia-rt.js` تغییر نکرد |

## ۲. ffmpeg (دستی، قبل از هارنس)
فرمت‌هایِ ساختگی (سینوسِ ۵ثانیه): wav، m4a(aac)، mp3، opus، amr، 3gp(amr)، mp4 (ویدیو+صدا)، flac، wma — همه با `-protocol_whitelist file -format_whitelist …` خوانده شدند؛ فایلِ تصادفی و playlistِ `#EXTM3U` رد شدند.

## ۳. UI — Browser pane + mock backendِ scratchpad (بدونِ حساب؛ `public/`ِ واقعی)
- مودال (تمِ تاریک): انتخابِ فایلِ ساختگیِ ۱۰٫۵MB ⇒ کارتِ فایل + تاریخِ پیش‌فرض از `lastModified` (۱۴۰۵/۰۶/۳۰)؛ دکمه تا تیکِ رضایت غیرفعال.
- شبکه‌ی ناپایدار (mock: هر تکه‌ی سوم ۵۰۳، هر اتصالِ پنجم قطع): فقط تکه‌ی ۲ دوباره ارسال شد؛ آپلود کامل.
- رفرشِ صفحه در ۶۰٪ یک فایلِ ۴۰MB: تکه‌هایِ ۰–۵ قبل و ۶–۹ بعد از رفرش، **هر کدام دقیقاً یک بار**؛ بدونِ انتخابِ دوباره‌ی فایل (IndexedDB).
- انتخابِ فایل در حالتِ آفلاین (شبیه‌سازیِ `navigator.onLine`) ⇒ «منتظرِ اینترنت» و `beforeunload` فعال ⇒ با رویدادِ `online` خودکار کامل شد.
- بعد از رفرش (شبیه‌سازیِ بستنِ تب) job در mock ادامه یافت؛ شمارنده‌ی زنگوله ۲ اعلانِ خوانده‌نشده؛ کلیک رویِ اعلان ⇒ صفحه‌ی جلسه با متنِ دوگوینده‌ای و stepperِ کامل.
- شکستِ رونویسی ⇒ کارتِ قرمز با پیامِ فارسی و «تلاشِ دوباره» ⇒ retry **بدونِ آپلودِ دوباره** (تعدادِ آپلودهایِ mock ثابت ماند) ⇒ بنرِ زنده‌ی «متن آماده شد».
- همان فایل دوباره ⇒ «قبلاً آپلود شده (جلسه ۳)» (یک باگِ UI در همین تست پیدا و رفع شد: کلیدِ «رسیدگی‌شده» فقط fingerprint بود).
- موبایل ۳۷۵px + تمِ روشن: بدونِ اسکرولِ افقی؛ سینی کاملاً مات (computed style: opacity 1، bg سفید).
- باگ‌هایِ پیدا/رفع‌شده در همین دور: سینی زیرِ بنر می‌رفت (z-index)، سینی با همان کلیکِ «شروعِ آپلود» بسته می‌شد، کلیدِ تکراری (بالا).
- Console: فقط ۵۰۳ِ عمدیِ mock.

## ۴. سرورِ واقعیِ dev + MySQLِ لوکال (با مجوزِ مالک)
- `pnpm dev` ⇒ `023_audio_upload_pipeline.sql applied` بدونِ خطا.
- `information_schema` (فقط خواندن): جدول‌هایِ `audio_uploads`/`audio_jobs`/`notifications`، ستون‌هایِ `session_audio.transcribed_at`/`client_case_file.content_version`، CHECKِ `sessions_source_check` = `live|manual|upload`؛ هر سه جدول خالی.
- بدونِ نشست: `GET /api/audio-jobs`، `GET /api/notifications`، `POST /api/uploads`، `PUT /api/uploads/x/chunks/0`، `POST /api/audio-jobs/x/retry` ⇒ همه ۴۰۱.
- صفحه‌ی `:3000` (با کوکیِ موجود در pane — هیچ عملی با آن حساب انجام نشد): `FeeliaUpload` لود، زنگوله نمایان، console بدونِ خطا.

## ۴.۱ تستِ کاملِ واقعی (دورِ دوم — دستورِ مالک: «کامل تست‌هاشو انجام بده»)

**محیط:** سرورِ dev + MySQLِ لوکال + **Sonioxِ واقعی** + **LLMِ واقعی (OpenRouter)** + ffmpegِ واقعی. داده کاملاً ساختگی:
گفت‌وگویِ ۴۳ثانیه‌ایِ دوصدایی با TTSِ ویندوز (David/Zira، انگلیسی)، از آن: m4a، AMR/3gp، ویدیوی mp4، WAVِ ۷ دقیقه‌ای (۹ تکه)،
**WAVِ ۶۰ دقیقه‌ایِ ۶۳۵MB** (44.1kHz استریو)، سکوت، فایلِ خراب، PDF با پسوندِ mp3، ویدیوی بی‌صدا. Fixture: دو تراپیستِ QA که
مستقیم در DBِ لوکال درج شدند (بدونِ ثبت‌نام؛ `password_hash` نامعتبر ⇒ ورود با رمز غیرممکن؛ فقط توکنِ مستقیمِ تست) و در پایان
با تمامِ داده/فایل‌هایشان حذف شدند.

| # | سناریو | نتیجه |
|---|---|---|
| E1 | بدونِ نشست ⇒ ۴۰۱ | PASS |
| E2 | رضایت/۱GB/فرمت/اثرِانگشت/مراجعِ تراپیستِ دیگر/فایلِ خیلی کوچک | PASS (400/413/415/400/404/400) |
| E3 | تکه با حجم/شماره‌ی نادرست، sha خراب، دسترسیِ تراپیستِ دیگر، لغو ⇒ پوشه حذف | PASS |
| E4 | خراب ⇒ unreadable، PDF ⇒ not-audio، ویدیوی بی‌صدا ⇒ no-audio؛ هیچ جلسه‌ای ساخته نشد؛ پوشه‌ها حذف | PASS |
| E5 | قطعی وسطِ آپلود ⇒ ۴۰۹ `missing:[1,8]` ⇒ «رفرش» ⇒ `resumed` ⇒ **فقط** تکه‌هایِ ۱ و ۸ ⇒ complete؛ complete ِ دوباره همان job | PASS |
| E6 | همان فایل دوباره ⇒ duplicate، جلسه‌ی دوم ساخته نشد | PASS |
| E7 | happy path: نرمال‌سازی ۱۶ث، Soniox ۲۲ث، پرونده با LLMِ واقعی ۴۶ث (کل ۸۴ث)؛ متنِ ۵۸۷۹ نویسه با **۲ گوینده**؛ `duration_ms` ۴۲۶۲۱۰؛ نسخه ۱؛ اعلان‌ها دقیقاً `transcript_ready`، `case_file_updated`؛ آرشیو `audio/ogg`/`source=upload`؛ فایلِ خام حذف | PASS |
| E8 | فهرستِ واقعیِ Soniox: هیچ فایل/transcriptionِ آپلودی باقی نمانده (کلِ حساب قبل و بعد از همه‌ی تست‌ها: ۸ فایل / ۲۳ transcription — دست‌نخورده) | PASS (دورِ اول به‌خاطرِ نیازِ dev به `PROXY_URL` از اسکریپت شکست خورد؛ با کلاینتِ خودِ سرور دوباره اجرا شد) |
| E9 | APIِ jobها/اعلان‌ها، ایزولاسیونِ کامل از تراپیستِ دیگر (خواندن/علامت‌گذاری) | PASS |
| R | **فایلِ ۶۰ دقیقه‌ایِ ۶۳۵MB:** ۱۵۲ تکه در ۹٫۲ث (لوکال)، complete (الحاق + probe) ۹٫۴ث، نرمال‌سازی ۸۱ث ⇒ **۹٫۸MB Opus** (۶۵× کوچک‌تر)، مدت ۳۶۰۰۰۱۰ms. **سرور وسطِ رونویسی kill شد**؛ stage و هر دو شناسه‌ی Soniox در DB ماند؛ یک leaseِ ۲۰دقیقه‌ایِ «پروسه‌ی مرده» عمداً ست شد؛ بعد از start، متن **۱۰ ثانیه** بعد ثبت شد (lease در startup آزاد شد؛ آپلود/رونویسیِ دوباره‌ی ۶۰ دقیقه در ۱۰ث ممکن نیست ⇒ همان transcription ادامه یافت). متن ۴۹٬۵۹۵ نویسه، ۲ گوینده، نسخه ۱، مراجعِ فعال ⇒ پرونده `not_applicable`، فایلِ ۶۳۵MB حذف | PASS (توضیح: اسکریپتِ ناظر بعد از پایانِ job رسید؛ شواهد از timelineِ DB/لاگ) |
| C1 | `applyTranscriptOnce` × ۵ **هم‌زمان** ⇒ دقیقاً ۱ اعمال، ۱ اعلان | PASS |
| C2 | جلسه‌ی دارایِ متن ⇒ append با برچسب، نه جایگزینی | PASS |
| C3 | (F1) `applyBatchSegmentOnce` × ۵ هم‌زمان ⇒ ۱ append؛ retryِ بعد از کرش ⇒ false | PASS |
| C4 | (F6) upsert با نسخه‌ی کهنه ⇒ null، بدونِ نوشتن | PASS |
| C5 | (F6) `claimGenerating` × ۶ هم‌زمان ⇒ دقیقاً ۱ | PASS |
| mutation | حذفِ `FOR UPDATE`ها ⇒ C1: ۵ بار اعمال، C3: ۵ بار append (باگ بازتولید شد) ⇒ کد بازگردانده شد | تأیید شد که تست باگ را می‌گیرد |
| F6-real | یادداشتِ تازه ⇒ auto-trigger ِ واقعی ⇒ **حینِ** `generating` دو PATCHِ هم‌زمان (`mainIssue`، `overallStatus`) ⇒ بعد از پایانِ LLM هر دو ویرایش ماندند (`content_version` ۱→۴) | PASS |
| mutation F6 | برگرداندنِ منطقِ قدیم (snapshotِ شروع، بدونِ CAS) ⇒ ویرایشِ همین دور پاک شد و مقدارِ دورِ قبل برگشت ⇒ کد بازگردانده شد | باگِ F6 رویِ LLMِ واقعی بازتولید شد |
| FMT | AMR/3gp (ضبطِ اندروید) و ویدیوی mp4 ⇒ متنِ درست با ۲ گوینده (۱۰–۱۴ث) | PASS |
| SIL | فایلِ ساکت ⇒ done، بدونِ متن، اعلانِ `transcript_empty`، بدونِ پرونده | PASS |
| RETRY | jobِ شکست‌خورده ⇒ تراپیستِ دیگر ۴۰۴ ⇒ retry ⇒ مستقیم `transcribing` (بدونِ آپلود/نرمال‌سازی) ⇒ retryِ دوم ۴۰۹ `not-failed` ⇒ متن ⇒ اعلانِ شکست حذف و `transcript_ready` ⇒ بعد از حذفِ صدا retry ⇒ ۴۱۰ `audio-expired` | PASS |
| F8-real | صفِ batch با `late-transcript` رویِ جلسه‌ی completed (Sonioxِ واقعی) ⇒ یک append با برچسب، `transcribed_at` ست، **پرونده خودکار بازتولید شد** | PASS |
| DEL | حذفِ جلسه‌ی آپلودی ⇒ آرشیو پاک؛ حذفِ مراجع با آپلودِ نیمه‌کاره ⇒ پوشه‌ی تکه‌ها پاک | PASS |
| SW1–3 | آپلودِ ۸روزه ⇒ canceled/expired + حذف؛ ۱روزه دست‌نخورده؛ پوشه‌ی یتیم حذف | PASS |
| UI-real | Browser pane رویِ **سرورِ واقعی** (`127.0.0.1` تا کوکیِ `localhost` ِ pane دست نخورد) با حسابِ QA: مودال ⇒ کلیکِ واقعیِ «شروعِ آپلود» ⇒ بنرِ زنده‌ی «متن … آماده شد» ⇒ بنرِ «پرونده … به‌روز شد» (LLMِ واقعی) ⇒ کلیک رویِ اعلان ⇒ صفحه‌ی جلسه با متنِ دوگوینده‌ای و stepperِ کامل | PASS |

**باگ‌هایِ UIِ پیداشده در همین دور و رفع‌شده (هر سه با نمایشگرِ خیلی باریک/واقعی):** (۱) نامِ فایل وسطِ کلمه می‌شکست
(`word-break:break-all`) ⇒ یک‌خطی با ellipsis و دکمه‌ی «تغییرِ فایل» زیرِ حجم؛ (۲) اسکرولِ افقی در سینی در عرضِ ۲۸۴px (مراحلِ
عرضِ ثابت) ⇒ مراحلِ انعطاف‌پذیر؛ (۳) سینی هر ۵ ثانیه کلِ DOM را بازمی‌ساخت ⇒ کلیکِ کاربر ممکن بود گم شود ⇒ بازسازی فقط در
تغییرِ واقعیِ محتوا (تأیید: همان node بعد از ۱۱ ثانیه).

**مشاهده‌ی قبلی/نامرتبط (رفع نشد):** متنِ انگلیسی در `.transcript-box` (RTL) علامتِ سؤال را به ابتدایِ جمله می‌برد («?week») — مسئله‌ی
bidiِ کادرِ متنِ موجود، نه این فیچر؛ متنِ فارسی سالم است.

**بعد از همه:** `tsc` تمیز، `test:up` 24/24، `test:cf` 108/108، syntaxِ فرانت OK؛ fixtureها (۲ تراپیست، ۱۳ جلسه، ۱۶ آپلود، ۷ پوشه)
حذف شدند و `remaining` صفر؛ `data/uploads` خالی؛ اسکریپت‌هایِ موقتِ `server/e2e-tmp` حذف شدند.

## ۵. انجام نشد (صادقانه — به‌روزشده بعد از دورِ دوم)
- **حافظه‌ی سرور** حینِ آپلودِ ۶۳۵MB اندازه‌گیری نشد (طراحی stream است؛ اندازه‌گیری نشد).
- **گفتارِ فارسی** تست نشد (TTSِ فارسی رویِ این ماشین نیست) — متنِ انگلیسیِ ساختگی.
- اینترنتِ واقعاً ضعیف (فقط شبیه‌سازی: قطعِ عمدیِ تکه‌ها در API، ۵۰۳/قطعِ اتصال در mock).
- `sweepSonioxOrphans` عمداً اجرا نشد (حسابِ Soniox با production مشترک است).
- کانفیگِ زنده‌ی nginx و production.

## ۵.۰ (نسخه‌ی دورِ اول — تاریخی)
- مسیرِ کاملِ واقعی `upload → ffmpeg → Soniox → متن → پرونده → اعلان` رویِ DB (نیازمندِ حسابِ canary + Soniox — مجاز نشد).
- ری‌استارتِ واقعیِ سرور وسطِ رونویسی (فقط در هارنس).
- idempotencyِ واقعیِ SQL (`applyTranscriptOnce`، `applyBatchSegmentOnce`، CASِ `content_version`، `claimGenerating`) رویِ MySQL — منطق خوانده و type-check شد، اجرا نشد.
- فایلِ واقعیِ ۱ ساعته/۱GB (بزرگ‌ترین تستِ واقعی: ۴۰MBِ ساختگی در mock؛ نرمال‌سازیِ ۳۰ثانیه‌ای با ffmpeg).
- کانفیگِ زنده‌ی nginxِ production.
