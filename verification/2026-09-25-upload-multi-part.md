# Verification — آپلودِ چند فایل برایِ یک جلسه + آیکونِ حذف (2026-09-25)

> Evidence (مشاهده در یک زمان). مالکِ رفتار: [subsystem 06 §۲.۱](../docs/07-subsystems/06-audio-upload-pipeline.md).

## درخواستِ مالک
1. حینِ آپلودِ ویس یک SVG برایِ حذف باشد.
2. بشود چند ویس (تکه‌تکه) آپلود کرد؛ «ویس‌ها مربوط به همان جلسه‌اند و به ترتیبِ آپلود ترنسکریپت شوند».

## تست‌ها

| تست | نتیجه |
|---|---|
| `cd server && npx tsc --noEmit` | تمیز |
| `pnpm test:up` | **35 PASS / 0 FAIL** (۳۱ قبلی + H30، H31، H33، H34 تازه) |
| H30 (ماشینِ حالت، portِ جعلی) | ۳ بخش: probeِ هر بخش به ترتیب، normalize با آرایه‌ی مسیرها به همان ترتیب و مدتِ جمع، حذفِ پوشه‌یِ هر ۳ بخش، `sourceParts` پاک، یک متن و یک اعلان |
| H31 | مجموع > ۳۰۰ دقیقه ⇒ `too-long` بدونِ normalize؛ یک بخشِ گم‌شده ⇒ `audio-missing` |
| H33 (ffmpeg 8.1.2 واقعی) | m4aِ استریو ۴۴k (۴ث) + wavِ mono 8k سکوت (۳ث) + ویدیوی mp4 (۴ث) ⇒ یک Ogg/Opus ~۱۱ث؛ `volumedetect`: ثانیه‌ی ۱ صدا، ثانیه‌ی ۵ سکوت، ثانیه‌ی ۸.۵ صدا ⇒ **ترتیب حفظ شد**؛ ترتیبِ معکوسِ آرایه ⇒ خروجیِ معکوس |
| H34 | بخشِ playlist (`#EXTM3U`) در میانِ بخش‌ها ⇒ شکست (whitelist برایِ هر ورودی) |
| UI در Browser pane با mock backendِ scratchpad (داده‌ی canary، بدونِ حساب/DB) | مودال: ۳ فایل در دو انتخاب، فایلِ تکراری و PDF با پیامِ فارسی رد شدند؛ کلیکِ واقعی رویِ سطل ⇒ حذف؛ دکمه‌ی «پایین» ⇒ جابه‌جایی؛ دکمه «شروعِ آپلودِ ۳ فایل» |
| | آپلود: `POST uploads → complete` بخش‌ها **پشتِ‌سرِ‌هم** (نه موازی)؛ `part_index` مطابقِ ترتیبِ مودال؛ یک job با `parts_total=3` |
| | سینی: «بخشِ ۱ از ۲ · …» در حالِ آپلود، بخشِ ۲ «در صف»؛ آیکونِ سطل با عنوانِ «لغوِ آپلودِ همه‌ی بخش‌هایِ این جلسه» |
| | کلیکِ سطل وسطِ بخشِ ۳ (بخش‌هایِ ۱–۲ رسیده) ⇒ یک `DELETE /api/upload-groups/…`، هر ۳ بخش `canceled`، هیچ درخواستی بعد از لغو، کارت‌ها پاک |
| | رفرشِ صفحه وسطِ بخشِ ۱ ⇒ ادامه از IndexedDB، گروه با **همان ۲ آپلود** (بدونِ تکرار) کامل شد و یک job |
| | تک‌فایلی: مسیرِ قبلی (`group_id` null)، آیکونِ سطل «لغو و حذفِ این آپلود» |
| | عرضِ ۳۷۵px: `scrollWidth = 375` (بدونِ اسکرولِ افقی)؛ نامِ طولانی کوتاه می‌شود؛ خطای console: هیچ |

## E2E رویِ MySQLِ dev (همان روز، با مجوزِ مالک: «اره تستِ E2E رو انجام بده») — **16 PASS / 0 FAIL**

روش: اسکریپتِ موقتِ tsx در `server/e2e-tmp/` (بعد از اجرا حذف شد)، Fastify با `registerAuthContext` + `audioUploadRoutes`،
`app.inject` با کوکیِ نشست؛ fixtureهایِ canary (دو تراپیست با password_hashِ غیرقابلِ‌استفاده و ایمیلِ `.invalid`، دو مراجع با رضایتِ ثبت‌شده)؛
فایل‌هایِ صوتیِ ساختگی با ffmpeg؛ **Sonioxِ جعلی** (هیچ فراخوانیِ واقعی/کلید). سرورِ dev روشن نبود ⇒ workerِ دیگری jobها را برنمی‌داشت.
`runMigrations()` ⇒ **migration 025 رویِ DBِ dev اعمال شد**.

| # | سناریو | نتیجه |
|---|---|---|
| E1 | migration 025: ۵ ستون + ردیفِ `_migrations` | PASS |
| E2 | `bad-part`: ۱۱ بخش، index خارج از بازه، ۱ بخش، group_idِ نامعتبر | PASS |
| E3 | رسیدنِ نامرتب (بخشِ ۲ اول) ⇒ `part_done` (۱ از ۳)، بدونِ جلسه، `duration_ms` ثبت | PASS |
| E4 | POSTِ دوباره ⇒ `part_done`؛ fingerprintِ دیگر ⇒ 409 `part-mismatch` | PASS |
| E5 | مالکیت: complete/لغوِ گروه توسطِ تراپیستِ دیگر ⇒ 404 / `canceled:0`؛ مراجعِ دیگران ⇒ 404 | PASS |
| E6 | بخشِ ۰ ⇒ ۲ از ۳؛ بخشِ آخر ⇒ 201، یک جلسه (`source='upload'`، consent)، `source_parts` به ترتیبِ `part_index`، مدتِ جمع ~۸ث | PASS |
| E7 | complete/POSTِ دوباره‌ی هر ۳ بخش ⇒ همان job؛ فقط ۱ جلسه و ۱ job | PASS |
| E8 | jobِ failed پیش از نرمال‌سازی ⇒ retry (همه‌ی بخش‌ها موجود) ⇒ `normalizing` | PASS |
| E9 | اجرایِ job با storeِ SQL + ffmpeg + آرشیوِ واقعی: `done`، یک `session_audio`، متن یک بار، یک اعلانِ `transcript_ready`، پوشه‌یِ هر ۳ بخش حذف؛ **volumedetect: ۰–۳ث تُن، ۳–۵ث سکوت، ۵–۸ث تُن ⇒ ترتیبِ `part_index` حفظ شد با اینکه بخشِ ۲ زودتر رسیده بود** | PASS |
| E10 | لغوِ گروه (یک بخشِ رسیده + یک بخش در حالِ آپلود) ⇒ `canceled:2`، پوشه‌ها حذف، ادامه ⇒ `group-closed`؛ گروهِ ساخته‌شده ⇒ `canceled:0` | PASS |
| E11 | بخشِ PDF ⇒ 422 `not-audio`، بعد گروه ⇒ `group-closed` | PASS |
| E12 | مجموع > ۳۰۰ دقیقه ⇒ 422 `too-long`، همه‌ی بخش‌ها `failed`، جلسه‌ای ساخته نشد | PASS |
| E13 | sweep: بخشِ منتظر > ۷ روز ⇒ `canceled/expired` + حذفِ پوشه؛ منتظرِ تازه دست‌نخورده | PASS |
| E14 | رگرسیونِ تک‌فایلی: فیلدهایِ گروه null، `source_parts` null؛ رفتارِ B2 بدونِ تغییر | PASS |
| E15 | تک‌فایلیِ هم‌محتوا با یک بخشِ گروه duplicate حساب نمی‌شود | PASS |
| E16 | هم‌زمانی: completeِ هم‌زمانِ هر ۳ بخش، ۳ دور ⇒ هر بار دقیقاً یک 201، یک جلسه، یک job | PASS |

پاک‌سازی: `DELETE FROM therapists` (cascade) ⇒ ۰ تراپیست/آپلود/مراجع باقی؛ ۰ پوشه در `data/uploads` و `data/session-audio`؛ ۰ آپلودِ یتیم؛ ۰ رویدادِ obsِ یتیم در ۳۰ دقیقه‌ی اخیر.

## تست‌نشده (صادقانه)
- رونویسیِ واقعی با Soniox رویِ فایلِ وصل‌شده (مرحله‌ی Soniox همان کدِ تک‌فایلی است؛ فقط ورودیِ آن تغییر کرده).
- مسیرِ کاملِ مرورگر ← سرورِ واقعی (UI با mock تست شد، سرور با inject).
- deploy نشده؛ commit نشده.
