# Verification — نکات کلیدی + digestِ بدونِ فکت — 2026-09-20

> Evidence (مشاهده در یک زمان). دادهٔ کاملاً ساختگی (LAW-001).

| بررسی | نتیجه |
|---|---|
| `tsc --noEmit` | تمیز |
| `pnpm test:cf` | 74 PASS / 0 FAIL (L1–L11، M1–M6، N1 جدید) |
| mutation: resolve | 5 FAIL (L1، L2، L4، L5، L6)؛ برگردانده شد |
| mutation: سقفِ ۳ | L7 FAIL؛ برگردانده شد |
| mutation: merge بدونِ حفظِ ویرایش | L6 FAIL؛ برگردانده شد |
| mutation: چکِ facts خالی | M1، M4، M5 FAIL؛ برگردانده شد |
| `pnpm test:rt` | 35 PASS / 0 FAIL |
| UI (mock با `applyFieldPatch` واقعی) | ۲ کارت قبل از «دارو»؛ pin سوم ثبت؛ pin چهارم رد («حداکثر 3») بدونِ تغییرِ وضعیت؛ unpin؛ ۳ PATCH در لاگ؛ پرونده‌ی قدیمی/شناسه‌ی معلق/ویرایش‌شده‌ی خالی ⇒ بخش پنهان؛ کنسول بدونِ خطا؛ `overflowX=false` |
| مدل واقعی #۱ | `digestFacts=0` ⇒ `keyPoints=0` (شکستِ خاموش؛ رفع شد)؛ 271ث |
| مدل واقعی #۲ | 21 فکت/14 نقل؛ **3 نکته‌ی کلیدی وصل شد**؛ 10/10؛ 346ث؛ retryِ compose به‌خاطرِ بودجه رد شد |

## محدودیت‌ها
- Browser pane پنهان ⇒ قضاوتِ بصری انجام نشد.
- کیفیتِ انتخابِ نکات فقط prompt است: یک نکته‌ی کلیدی «ردِ افکارِ خودکشی» بود (بهتر در محورِ «ایمنی»)؛ نمونه‌ی کم.
- رفعِ digestِ بدونِ فکت با یک retry است؛ اگر retry هم خالی باشد، پرونده بدونِ ردیابیِ مکانیکی ساخته می‌شود (فقط log).
- زمانِ تولید ۲۷۱–۳۴۶ث در این دو اجرا.
